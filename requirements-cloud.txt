fastapi>=0.104.0
uvicorn>=0.24.0
psycopg2-binary>=2.9.9
pydantic>=2.7,<3.0
PyYAML>=6.0,<7.0
python-dotenv>=1.0,<2.0
# jsonschema — required transitively by src/api/cloud_routes/system_index.py
# (imports capability_registry which validates Action schemas via jsonschema).
# Missing → cloud deploy crashed on startup with ModuleNotFoundError.
jsonschema>=4.26.0,<5.0
# numpy — required by src/api/cloud_routes/kpis.py (PR-690 I3: Lo (2002)
# autocorrelation-corrected Sharpe SE uses np.corrcoef + np.array slicing).
numpy>=1.24,<3.0
# requests — required transitively by src/api/cloud_routes/kpis.py via
# src.api.cloud_routes.kpis_compute._compute_per_trade_rf, which imports
# get_rf_rate from src.data_ingestion.risk_free_rate (FRED fetch path).
# Missing → /api/kpis crashed with ModuleNotFoundError on every call.
# Recurrence of the jsonschema/numpy bug class.
requests>=2.31,<3.0
# scipy — required transitively by src/api/cloud_routes/analytics.py via
# src.evaluation.statistics.calmar_ratio (Sprint 3 T1 cockpit-coherence —
# Calmar canonical helper refactor at #987). Missing → cloud deploy crashed
# on startup with ModuleNotFoundError. Fourth recurrence of this bug class
# (jsonschema → numpy → requests → scipy).
scipy>=1.11,<2.0
# urllib3 — required transitively by src/notifications/telegram.py (Sprint 4 T3
# safe_send wrapper imports `urllib3.exceptions` to catch HTTPError specifically;
# cloud_app reaches notifications/telegram.py via cloud_routes/platform.py which
# calls platform_events.notify_*). urllib3 ships transitively via requests today,
# but pip resolution drift could drop it; declaring explicitly per defensive policy.
# 6th recurrence of cloud-deploy import drift bug class — caught by T7 fast-lane
# AST walker as a self-reported failure on Sprint 4 Wave 1 PR #1020 review.
urllib3>=2.0,<3.0
# tzdata — required transitively by src.scheduler.holidays + various date logic
# via zoneinfo.ZoneInfo('America/New_York'). Linux Render uses OS tzdata from
# /usr/share/zoneinfo so the gap was masked in production; surfaces on Windows
# clean venv. Sprint 4 T8 caught this — 5th recurrence of the cloud-deploy
# import drift bug class. NOTE: T7's fast-lane AST walker did NOT catch this
# because zoneinfo loads tzdata via runtime string lookup, not an import
# statement (out of T7's design scope). T8 slow-lane venv subprocess was the
# detection vector.
tzdata>=2024.1,<2026
