# T2.18 Deferral Note — 2026-04-25

## Decision

**Defer T2.18 ("Remove plugin interface, -300 LOC") from Cohort 1 to a future cohort with redesigned scope.**

## Reason

The audit spec (`audit-spec.md` §6.6) and plan entry (`plan.md` T2.18) both stated:

> Grep zero `@register_plugin` in src/.

Pre-deletion grep at PM dispatch time (commit `03476f3`) found:

```
src/platform/plugin_registry.py:14:from src.platform.strategy_plugin import StrategyPlugin
src/platform/plugin_registry.py:23:        @register_plugin
src/platform/signal_eval.py:291:    from src.platform.plugin_registry import get_plugin
src/platform/signal_eval.py:292:    from src.platform.strategy_plugin import Candidate
src/platform/signal_eval.py:310:    f"import the module with @register_plugin before calling."
src/platform/strategy_plugin.py:42: ...
tests/platform/test_signal_eval_python_plugin.py:13:from src.platform.plugin_registry import (
```

The audit spec **missed** that `src/platform/signal_eval.py:285-329` defines `_find_candidates_python_plugin()`, dispatched from `signal_eval.py:192-193` whenever a spec has `kind: python_plugin`. While *no current production spec* uses `kind: python_plugin` (only `event_driven` and `mechanical` are in `src/platform/specs/*.yaml`), the dispatch path is reachable and tested by 13 tests in `tests/platform/test_signal_eval_python_plugin.py`.

T2.18's `scope_fence` is explicit: "If non-test consumer found, ESCALATE — do NOT leave broken imports."

## Scope Required for Future T2.18

A correctly-scoped T2.18 must remove all of:
1. `src/platform/strategy_plugin.py` (71 lines)
2. `src/platform/plugin_registry.py` (48 lines)
3. `src/platform/signal_eval.py:285-329` (the `_find_candidates_python_plugin` function, ~45 lines)
4. `src/platform/signal_eval.py:192-193` (the `if kind == "python_plugin"` dispatch case)
5. `tests/platform/test_signal_eval_python_plugin.py` (330 lines, 13 tests) — replaced by a single negative test in `tests/platform/test_plugin_removal.py` that asserts `kind: python_plugin` raises `ValueError` from the dispatch.

Net LOC removal: ~500. Net test floor change: ~−12 (delete 13, add ~1 negative).

## Pre-conditions for Re-dispatch

Before re-dispatching T2.18:
1. Audit spec amended to v3.2 with corrected file inventory
2. Test floor planned with explicit absorption of the 13-test loss (e.g., absorbed into a Cohort 3 net-add, or treated as a one-time floor adjustment)
3. Risk review: confirm no future strategy author will need `kind: python_plugin` (Cohort 3 strategy redesign is on the methodology side, not the plugin side, so this should be fine)

## Cohort 1 Status

T2.18 is deferred but Cohort 1 is otherwise complete:

| Task | Status | Commit |
|------|--------|--------|
| T2.06 — PBO writer | ✅ committed | `f9c261d` |
| T2.10 — FRED rf-rate + auto_adjust pin | ✅ committed | `940aad3` |
| T2.11 — pandas_market_calendars NYSE | ✅ committed | `8afd238` |
| T2.13 — drop dead classifier classes | ✅ committed | `e811f07`, `5ab8e3b` |
| T2.15 — lazy-prices spec shelving | ✅ committed | `d8d12e2` |
| T2.17 — fail-closed governor + is_connected | ✅ committed | `03476f3` |
| **T2.18 — plugin interface removal** | **⏸ deferred** | — |
