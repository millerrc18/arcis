# Arcis #107 Tier 3 Tools — Implementation Plan

**Spec:** [`docs/audits/2026-05-25-tier3-tools/specs/2026-05-25-tier3-tools-design.md`](../specs/2026-05-25-tier3-tools-design.md)
**Target release:** v0.36.6X (re-baseline at implementation time; current main is v0.36.64)
**Estimated effort:** ~1 day agent work + dual-Opus QA
**Tasks:** 8 across 5 execution batches
**3 tools: ContractCheck v1 (nvidia-smi watchloop) + GitArchaeology (read-only) + DocConsistency v1 (file:line refs)**

---

## Implementation Discipline (read first)

Sequencing rationale:\n\nBatch 1 (T1 alone): foundation extension must land first — T2/T3/T4 all import from the extended `_config.py` and `_subprocess.py`. Cannot parallelize because the three tools share these files.\n\nBatch 2 (T2, T3, T4 in parallel): three independent subpackages, no shared files between them. Maximally parallel. CRITICAL constraint for all three: use `@safe_op(name=\"<tool>\", mutates=False)` — NOT `@safe_op(write=False)`, NOT `@safe_op(tool=..., op=...)`. Wrong kwargs produce TypeError at module-import time. Verified-correct signature per src/tools/_safety.py:167-172.\n\nBatch 3 (T5 alone): records the live baseline using ContractCheck (depends on T2). Cannot parallelize with T6/T7 because T6's `test_verify_against_committed_baseline` reads the file T5 produces.\n\nBatch 4 (T6, T7 in parallel): tests are independent across the three tools. All three test files use the `_integration.py` suffix per Tier 1+2 convention.\n\nBatch 5 (T8 alone): CHANGELOG + version + final verification depends on everything else.\n\nDual-Opus QA gate (per operator memory feedback_use_coding_team_skill): after all 8 tasks complete, two independent Opus reviewers verify root-cause / hardening / ripple / noise at 100% confidence before merge.\n\nLatent finding tracking: task #117 (Restore [N/A] defense in system_metrics.py nvidia-smi parser) is already in the operator's task list as pending. The PR description for #107 must include a one-line callout: 'Surfaces #117 — system_metrics.py:36-56 [N/A] gap discovered during ContractCheck design; out-of-scope for this PR.' Operator's no-out-of-scope-deferral discipline is preserved at the *effort boundary* (file as own effort, don't quietly punt) — not at the 'fix everything you find' interpretation.\n\nIf T5 is scoped-out at impl time (no nvidia-smi on the build agent's machine), T6 still passes (its tests use mocked subprocess.run) and operator records the baseline manually post-merge. Operator-visible PR-description annotation required in this case.\n\nR2 (DA-revision pass — execution_order UNCHANGED) corrections rolled in:\n- DA1 (critical): T1 spec §3.2 seed uses recalibrated tolerances (gpu_util_pct:5.0 / vram_used:2048 / temp:10 / power:50); T2 scope_fence enforces no-revert to wide values; T6 adds `test_verify_drift_value_mismatch` confirming value-drift detection at recalibrated tolerances.\n- DA2 (critical): T1 ContractsConfig extension includes `at_capture_redact: list[str]` field on NormalizeRule; T2 record()/verify() applies recording-time + verify-time redaction; T6 adds `test_record_redacts_at_capture_when_configured`.\n- DA3 (major): T3 log() implementation uses `line.split('\\t', N-1)` with explicit maxsplit; T3 enforces format=/format_columns= paired kwargs; T3 raises GitParseError on malformed output; T7 adds 4 tests (`test_log_subject_with_embedded_tab`, `test_log_custom_format_requires_columns`, `test_log_custom_format_with_columns`, `test_log_parse_failure_raises`).\n- DA4 (major): T3 _git() applies per-op max_output_bytes post-invocation check raising GitOutputTruncatedError; T3 blame() pre-invocation gate for >5000-line files; T3 CLI accepts `--max-output-bytes N`; T7 adds 5 tests (`test_blame_large_file_requires_range`, `test_blame_large_file_truncates_cleanly`, `test_show_respects_max_output_bytes`, `test_show_default_max_output_bytes`, `test_cli_max_output_bytes_flag`).\n- DA5 (major): no plan-task changes — spec-only framing rewrite (ContractCheck v1 is a FORENSIC tool, operator-invoked, not runtime-instrumented; periodic invocation deferred to §11.5 / #111). T6 north-star test purpose-statement clarified in description.\n\nR1 (prior revision) corrections rolled in:\n- FB1 (major): every task description now specifies `@safe_op(name=\"<tool>\", mutates=False)` — verified signature.\n- FB2 (minor): atomic-write idiom inherited from `src/tools/ciinvestigate/core.py:64`ff (CHANGELOG.md:107 documented the convention; the cache directory itself is runtime-created/gitignored).\n- FB3 (minor): all three test files renamed to `test_<toolname>_integration.py` per Tier 1+2 convention.\n- FB4 (minor): docs/standards/ confirmed existing (contains boundary-touch-tests.md); default-scope reference retained; T7 adds a test that exercises the directory inclusion.\n- FB5 (nit): §2.1 opening rephrased to remove no-modifications/yes-modifications contradiction.

---

## Execution Order

**Batch 1:** Task 1

**Batch 2:** Task 2, Task 3, Task 4

**Batch 3:** Task 5

**Batch 4:** Task 6, Task 7

**Batch 5:** Task 8

---

## Tasks

### Task 1 — Foundation extension (config + gitignore + allowlist seed)

**Estimated complexity:** low

**Files in scope:**
- `src/tools/_config.py`
- `src/tools/_subprocess.py`
- `config/arcis_config.yaml`
- `.gitignore`

**Files (read-only context):**
- `src/tools/_cli_envelope.py`
- `src/tools/_safety.py`

**Description:**

PREAMBLE (no out-of-scope deferral): within this effort deliver ALL three tools (T2-T4) and their tests (T6-T7), the initial baseline (T5), and CHANGELOG/version (T8). Do NOT punt sub-items to follow-up memory. The latent system_metrics.py:36-56 [N/A] gap is OUT-OF-SCOPE for #107 — it is filed as #117 and surfaces naturally when ContractCheck records the live baseline; that is the correct outcome.\n\nThis task extends the foundation to support Tier 3:\n1. Append `NormalizeRule`, `ContractDef`, and `ContractsConfig` pydantic models to `src/tools/_config.py` after PgConfig (L114). Extend `ArcisConfig` (L116-123) with `contracts: dict[str, ContractDef] = Field(default_factory=dict)`. Backward compat is mandatory — existing arcis_config.yaml files without `contracts:` must still load.\n2. Append two new typed errors (`GitMissingError`, `NvidiaSmiMissingError`) to `src/tools/_subprocess.py` mirroring the L31-37 precedent (NssmMissingError / GhMissingError). Bump `@lru_cache(maxsize=4)` at L39 to `maxsize=6`. Extend `resolve_exe()` (L39-58) with elif branches for the two new names. Include install hints (e.g., `git: 'Install via winget install Git.Git'`).\n3. Append the `contracts:` section to `config/arcis_config.yaml` after L146 (after pg section). Seed with the single `nvidia-smi-watchloop` contract entry — see spec §3.2 for exact YAML.\n4. Append THREE lines to `.gitignore` after L42 (after the existing `!data/reference/` allowlist): `!data/contracts/`, `!data/contracts/**`, `!data/docconsistency-allowlist.yaml`. Verify with `git check-ignore --verbose data/halcyon.db` (must still be ignored) and `git check-ignore --verbose data/contracts/foo/bar.json` (must NOT be ignored — exit 1).\n5. Create `data/docconsistency-allowlist.yaml` with the empty seed scaffold (spec §3.4) — a `# comment block` plus `allowlist: []`.\n\nAcceptance: existing Tier 1+2 tests still pass (run `pytest tests/tools/ -x` once after the change); pydantic loads the extended config; gitignore checks pass.

**Test strategy:**

Run `pytest tests/tools/test_config_integration.py tests/tools/test_subprocess_integration.py -v` after edit (or whatever the existing test filenames are — match the actual `_integration.py` Tier 1+2 convention). Backward compat: both must pass unchanged. Add one new assertion to test_config: load a YAML with no `contracts:` section and assert ArcisConfig.contracts == {}. Run `git check-ignore --verbose data/halcyon.db` (must exit 0 — ignored). Run `git check-ignore --verbose data/contracts/test/file.json` (must exit 1 — NOT ignored, allowlisted). Run `git check-ignore --verbose data/docconsistency-allowlist.yaml` (must exit 1).

**Scope fence:** Do NOT create any contractcheck/ / gitarchaeology/ / docconsistency/ files in this task — those are T2-T4. Do NOT modify _cli_envelope.py, _safety.py, _execution_log.py (foundation modules unchanged in #107). Do NOT touch the seed allowlist beyond the scaffold (operator curates entries). Do NOT pre-record any baselines (that is T5). Do NOT touch any data/docconsistency-allowlist.yaml entries beyond the empty seed. Do NOT add Optional[ContractsConfig] wrapping — flat dict per safety_windows precedent.

---

### Task 2 — ContractCheck subpackage (core + CLI)

**Depends on:** Task 1

**Estimated complexity:** medium

**Files in scope:**
- `src/tools/contractcheck/__init__.py`
- `src/tools/contractcheck/__main__.py`
- `src/tools/contractcheck/core.py`

**Files (read-only context):**
- `src/tools/_config.py`
- `src/tools/_subprocess.py`
- `src/tools/_cli_envelope.py`
- `src/tools/_safety.py`
- `src/tools/capabilityregistry/__main__.py`
- `src/tools/ciinvestigate/core.py`

**Description:**

Implement `src/tools/contractcheck/` per spec §4.\n\nFiles:\n- `src/tools/contractcheck/__init__.py`: 5-section docstring; export `record`, `verify`, `diff` from `core.py`. ~15 LOC.\n- `src/tools/contractcheck/__main__.py`: argparse with three subparsers (record / verify / diff); each subcommand parses args and calls `run_cli('contractcheck', _run, args, json_mode=args.json)`. The `_run` dispatch helper picks the right function. Pattern matches `src/tools/capabilityregistry/__main__.py:63-80` verbatim. ~70 LOC.\n- `src/tools/contractcheck/core.py`: the three public functions + error classes + helpers. ~250 LOC.\n\nCore implementation requirements:\n- Define error classes: ContractCheckError (root), ContractNotConfiguredError, BaselineNotFoundError, BaselineCorruptError, ContractInvocationError. All subclass RuntimeError.\n- `record(name)`: load config via `load_arcis_config()`; lookup contracts[name] (raise ContractNotConfiguredError if missing); resolve nvidia-smi via `_subprocess.resolve_exe('nvidia-smi')` (raises NvidiaSmiMissingError if missing — propagates); call `_subprocess.run(contract.cmd, timeout=contract.timeout_s)`; parse via `_parse_fields` helper; write JSON to `data/contracts/<name>/<timestamp>.json` ATOMICALLY (tempfile + fsync + os.replace — inherit the idiom from `src/tools/ciinvestigate/core.py:64`ff; encoding='utf-8'; newline='\\n'; json.dumps with sort_keys=True, indent=2, ensure_ascii=False); update latest_ref.txt ATOMICALLY (same tempfile pattern); return Path of written baseline.\n- `verify(name)`: load config; load latest_ref.txt (raise BaselineNotFoundError if missing); load referenced baseline JSON (raise BaselineCorruptError if malformed); invoke live cmd; per-field comparison applying NormalizeRule (tolerance / mask_regex / ignore); compute verdict (PASS / DRIFT / INVOCATION_FAILED); return dict per spec §4.5.\n- `diff(name, baseline_a, baseline_b)`: load both baselines by filename within data/contracts/<name>/; per-field diff identical to verify but with baseline_a/baseline_b instead of live; return dict.\n- Timestamp format YYYY-MM-DDTHH-MM-SSZ (Windows-safe; colons stripped).\n- All Path operations use encoding='utf-8' explicit.\n\nDecorate the THREE public functions (record, verify, diff) with the verified-correct signature `@safe_op(name=\"contractcheck\", mutates=False)`. The decorator signature is `safe_op(*, name: str, mutates: bool, describe=None, log_path=None)` (verified at src/tools/_safety.py:167-172). DO NOT use `@safe_op(write=False)` — that kwarg does NOT exist on the decorator. DO NOT use `tool=`/`op=` kwargs — those do not exist. Reference call sites: src/tools/dbquery/core.py:162, src/tools/capabilityregistry/core.py:45,51. The `name` argument is the tool slug (the string \"contractcheck\"), not a per-operation identifier. All three operations are read-only in the audit-log sense; even `record` writes to data/contracts/ which is operator-state (auditable, git-tracked), not application-state — hence `mutates=False`. Refer to spec §10 for the cross-cutting standards.

**Test strategy:**

Unit tests are T6. This task delivers implementation only — no tests written here. After implementation, the developer agent runs `python -c 'from src.tools.contractcheck import record, verify, diff; print(record.__doc__)'` to smoke-test imports — this MUST succeed (would fail at import time if `@safe_op` kwargs are wrong, e.g., `write=False`). Mypy / pyright clean (no Optional[X] without explicit None; explicit return types on all public functions). Lint clean (ruff + black if configured).

**Scope fence:** Do NOT record an actual baseline in this task (that is T5). Do NOT modify foundation modules (those are T1). Do NOT touch any nvidia-smi caller in src/ (no fixes to system_metrics.py — that is #117 follow-up). Do NOT add tests in this task — tests are T6. Do NOT introduce new subpackage directories (single core.py is the operator's preferred shape per Tier 1/2 precedent). Do NOT use direct subprocess.run anywhere — go through _subprocess.run. Do NOT use `@safe_op(write=False)` / `@safe_op(tool=..., op=...)` — those kwargs DO NOT EXIST on the decorator; using them causes TypeError at module-import time. The correct signature is `@safe_op(name=\"contractcheck\", mutates=False)`. **DA1 enforcement:** the spec §3.2 contracts seed uses RECALIBRATED tolerances (gpu_util_pct:5.0, gpu_vram_used_mb:2048.0, gpu_temp_c:10.0, gpu_power_w:50.0); do NOT revert to wide-tolerance R1 values. **DA2 enforcement:** record() MUST apply `at_capture_redact` regexes from each field's NormalizeRule BEFORE writing the baseline JSON — iterate the normalize entries, collect at_capture_redact patterns, apply re.sub against baseline['stdout'] AND against any string-form parsed_fields values. Record `at_capture_redact: <N patterns>` in `normalization_applied`. verify() MUST re-apply the same redactions to live stdout BEFORE comparison so baseline-redacted vs live-redacted matches correctly.

---

### Task 3 — GitArchaeology subpackage (core + CLI, 7 ops)

**Depends on:** Task 1

**Estimated complexity:** medium

**Files in scope:**
- `src/tools/gitarchaeology/__init__.py`
- `src/tools/gitarchaeology/__main__.py`
- `src/tools/gitarchaeology/core.py`

**Files (read-only context):**
- `src/tools/_subprocess.py`
- `src/tools/_cli_envelope.py`
- `src/tools/_safety.py`
- `src/utils/deploy_info.py`
- `src/platform/rigor/walkforward_firewall.py`

**Description:**

Implement `src/tools/gitarchaeology/` per spec §5.\n\nFiles:\n- `src/tools/gitarchaeology/__init__.py`: 5-section docstring; export `log`, `blame`, `show`, `diff`, `rev_list`, `merge_base`, `tag_l` from `core.py`. ~25 LOC.\n- `src/tools/gitarchaeology/__main__.py`: argparse with 7 subparsers (one per op); each routes through `run_cli('gitarchaeology', _run, args, json_mode=args.json)`. The `_run` dispatch helper picks the right function via `args.cmd`. Forbidden ops are NOT registered as subparsers — they fail at argparse parse time with `invalid choice` (structural defense). Every subparser also accepts `--max-output-bytes N` (DA4) to override per-op size cap. ~150 LOC.\n- `src/tools/gitarchaeology/core.py`: 7 public functions + error classes + shared `_git()` helper + per-op MAX_OUTPUT_BYTES constants (DA4). ~350 LOC.\n\nCore implementation requirements:\n- Define error classes: GitArchaeologyError (root), GitInvocationError, GitArgError, **GitParseError (DA3)**, **GitOutputTruncatedError (DA4)**. GitMissingError comes from _subprocess (added by T1).\n- `_git(args, *, timeout_s, repo, max_output_bytes, op_name)` helper: resolves git exe via `_subprocess.resolve_exe('git')` (raises GitMissingError); prepends `-C <repo>` if repo specified; calls `_subprocess.run(full_args, timeout=timeout_s)`; checks returncode; on non-zero, parses stderr — if it contains 'fatal: detected dubious ownership' append the remediation hint `'(run: git config --system --add safe.directory C:/arcis/halcyon-lab)'` to the error message; raises GitInvocationError otherwise. After success, check len(result.stdout.encode('utf-8')) > max_output_bytes — if so, raise GitOutputTruncatedError(message, partial_output=stdout[:max_output_bytes_codepoint_safe], original_size_bytes, op=op_name).\n- `log(range=None, path=None, format='%H%x09%an%x09%ai%x09%s', format_columns=None, limit=50, repo=None, timeout_s=30, max_output_bytes=None)` (DA3+DA4 signature additions): if format_columns is None, infer default ['sha', 'author', 'date', 'subject']. If format != default AND format_columns is None → raise GitArgError('custom format= requires format_columns= kwarg'). If format_columns[-1] not in {'subject', 'body', 'message'} → raise GitArgError('format_columns last entry must be subject/body/message'). max_output_bytes defaults to limit * 200 if None. Builds argv as ['log', f'--format={format}', '-n', str(limit)] + ([range] if range else []) + (['--', path] if path else []); calls _git; for each output line use `line.split('\\t', len(format_columns) - 1)` with explicit maxsplit; if split returns fewer fields than len(format_columns) → raise GitParseError(offending_line, expected_columns, op='log').\n- `blame(file, start_line=None, end_line=None, repo=None, timeout_s=60, max_output_bytes=None)` (DA4 signature addition): validates start_line <= end_line if both given (raise GitArgError); if start_line/end_line both None AND target file >5000 lines (count via Path(file).read_text(encoding='utf-8').count('\\n')) → raise GitArgError('blame on >5000-line file requires start_line + end_line range'); max_output_bytes defaults to 2_000_000 if None; builds argv as ['blame'] + (['-L', f'{start_line},{end_line}'] if both else []) + ['--', file]; calls _git; parses porcelain or standard blame output into list of dicts with sha/author/content/line.\n- `show(sha, path=None, repo=None, timeout_s=30, max_output_bytes=None)` (DA4): max_output_bytes defaults to 10_000_000 if None; builds argv as ['show', sha] + (['--', path] if path else []); calls _git; parses commit metadata + diff sections into a dict.\n- `diff(ref_a, ref_b, path=None, repo=None, timeout_s=30, max_output_bytes=None)` (DA4): max_output_bytes defaults to 10_000_000 if None; builds argv as ['diff', f'{ref_a}..{ref_b}'] + (['--', path] if path else []); calls _git; returns stdout verbatim.\n- `rev_list(range, path=None, limit=None, repo=None, timeout_s=30, max_output_bytes=None)` (DA4): max_output_bytes defaults to (limit * 50 if limit else 2_500_000) if None; builds argv as ['rev-list'] + ([f'-n', str(limit)] if limit else []) + [range] + (['--', path] if path else []); returns list of {'sha': ...}.\n- `merge_base(ref_a, ref_b, repo=None, timeout_s=10)`: argv ['merge-base', ref_a, ref_b]; returns stripped stdout (single SHA).\n- `tag_l(pattern=None, repo=None, timeout_s=10)`: argv ['tag', '-l'] + ([pattern] if pattern else []); returns list of {'tag': ...}.\n- Decorate all 7 public functions with the verified-correct signature `@safe_op(name=\"gitarchaeology\", mutates=False)`. The decorator signature is `safe_op(*, name: str, mutates: bool, describe=None, log_path=None)` — verified at src/tools/_safety.py:167-172. DO NOT use `@safe_op(write=False)` — that kwarg does NOT exist. DO NOT use `tool=`/`op=` — they don't exist either. Reference call sites: src/tools/symbolfind/core.py:153, src/tools/dbquery/core.py:162. The `name` argument is the tool slug (the literal string \"gitarchaeology\"), not per-op identifiers — the `safe_op` decorator infers the function name automatically via the wrapped __name__.\n- FORBIDDEN ops are NOT exposed: no commit, push, reset, rebase, checkout, branch-D, clean-f, cherry-pick, stash-drop, tag-d subcommand registered. Document the forbidden list in `__init__.py`'s docstring verbatim.

**Test strategy:**

Unit tests are T7. This task delivers implementation only. Developer smoke-tests via `python -m src.tools.gitarchaeology --help` (should list 7 subcommands), `python -m src.tools.gitarchaeology log --limit 3` (should return 3 most-recent commits in TSV form against the live repo), `python -m src.tools.gitarchaeology commit` (must exit 2 with 'invalid choice: commit' — proves forbidden-by-construction). The help command's success transitively proves all 7 `@safe_op` decorators imported without TypeError. Mypy / lint clean.

**Scope fence:** Do NOT add ANY mutating ops (no commit, push, reset, rebase, checkout, branch-D, clean-f, cherry-pick, stash-drop, tag-d). Do NOT add rev-parse, config, or remote-v in v1 (those are §11.4 follow-ups). Do NOT refactor any of the existing 4 direct-git sites in src/ (deploy_info.py, walkforward_firewall.py, walkforward_runner.py, backtest_engine.py) — broader displacement is post-#107. Do NOT use direct subprocess.run / check_output anywhere — every git invocation goes through _subprocess.run via the _git helper. Do NOT write tests here. Do NOT use `@safe_op(write=False)` / `@safe_op(tool=..., op=...)` — wrong signature, will TypeError at import. Use `@safe_op(name=\"gitarchaeology\", mutates=False)`. **DA3 enforcement:** do NOT use unbounded `line.split('\\t')` — always pass explicit maxsplit (`len(format_columns) - 1`). Do NOT silently skip malformed log lines — raise GitParseError. Do NOT accept custom `format=` without paired `format_columns=` — raise GitArgError. **DA4 enforcement:** do NOT skip the pre-invocation 5000-line gate for blame. Do NOT skip the post-invocation size check — every _git call must check len(stdout.encode('utf-8')) against the op's max_output_bytes and raise GitOutputTruncatedError on overrun. Do NOT truncate output silently in the parser — let the structured error propagate so callers can catch + decide.

---

### Task 4 — DocConsistency subpackage (core + CLI, class (a) only)

**Depends on:** Task 1

**Estimated complexity:** medium

**Files in scope:**
- `src/tools/docconsistency/__init__.py`
- `src/tools/docconsistency/__main__.py`
- `src/tools/docconsistency/core.py`

**Files (read-only context):**
- `src/tools/_config.py`
- `src/tools/_cli_envelope.py`
- `src/tools/_safety.py`
- `src/tools/testpatternscan/rules.py`

**Description:**

Implement `src/tools/docconsistency/` per spec §6.\n\nFiles:\n- `src/tools/docconsistency/__init__.py`: 5-section docstring; export `scan` from `core.py`. ~12 LOC.\n- `src/tools/docconsistency/__main__.py`: argparse with one subcommand `scan` accepting `--target` (repeatable), `--allowlist`, `--json`; routes through `run_cli('docconsistency', _run, args, json_mode=args.json)`. ~60 LOC.\n- `src/tools/docconsistency/core.py`: scan function + regex + verification + default-scope discovery + allowlist loader + error classes. ~250 LOC.\n\nCore implementation requirements:\n- Define error classes: DocConsistencyError (root), DocConsistencyAllowlistError, DocConsistencyTargetMissingError.\n- `_REF_PATTERN`: regex per spec §6.4 supporting Patterns A and B (single line / line range with optional backticks); covers .py / .md / .yaml / .yml / .json / .sql / .toml / .ini / .cfg / .sh / .js / .ts / .html extensions. Pattern C (comma-list) is INTENTIONALLY not matched in v1.\n- `_default_targets(repo_root)`: returns list of paths covering CHANGELOG.md (always), README.md, every .md in docs/standards/ (the directory exists and currently contains boundary-touch-tests.md; glob gracefully returns [] if a future refactor empties it), docs/operator-guide.md, docs/cli-reference.md, every .md in docs/audits/ whose `os.path.getmtime` returns a timestamp within the last 90 days. Use `os.path.getmtime` (NOT GitArchaeology) for age filtering — avoids cross-tool dep and Windows-NTFS quirks.\n- `_load_allowlist(path)`: opens YAML with encoding='utf-8'; yaml.safe_load; expects top-level `allowlist: [str]`; missing file → returns empty list; malformed YAML → raises DocConsistencyAllowlistError.\n- `scan(targets=None, *, allowlist_path=None, repo_root=None)`: derives repo_root from __file__ if None; targets via _default_targets if None; allowlist_path via Path('data/docconsistency-allowlist.yaml') if None; for each target file (encoding='utf-8' on read), iterate _REF_PATTERN.finditer over each line, build the normalized ref string (no backticks: `f'{path}:{line}'`); allowlist check (cheapest first); file-exists check; line-count check; emit findings.\n- Return dict per spec §6.2.\n- Decorate `scan` with the verified-correct signature `@safe_op(name=\"docconsistency\", mutates=False)`. The decorator signature is `safe_op(*, name: str, mutates: bool, describe=None, log_path=None)` (src/tools/_safety.py:167-172). DO NOT use `@safe_op(write=False)` — that kwarg does NOT exist. DO NOT use `tool=`/`op=` — non-existent. Reference call sites: src/tools/dbquery/core.py:162, src/tools/symbolfind/core.py:153. The `name` argument is the tool slug (literal string \"docconsistency\").\n- Encoding='utf-8' explicit on every Path.read_text call.

**Test strategy:**

Unit tests are T7. Developer smoke-tests via `python -m src.tools.docconsistency scan --json` (must return valid JSON; the live CHANGELOG.md will produce some findings — that's expected). Smoke confirms `@safe_op` decorator imported without TypeError (would fail with `write=False`-style mistakes). Mypy / lint clean.

**Scope fence:** Do NOT implement class (b), (c), or (d) — only file:line existence (class a). Do NOT use AST parsing / find_spec / import_module — the v1 check is plain file-line-count. Do NOT couple to GitArchaeology (avoid circular import risk and Windows quirks — use os.path.getmtime for age filtering). Do NOT match Pattern C (comma-list refs) — those are v2 deferral. Do NOT pre-curate the allowlist — keep `data/docconsistency-allowlist.yaml` at its empty seed. Do NOT write tests here. Do NOT use `@safe_op(write=False)` / `@safe_op(tool=..., op=...)` — wrong signature; will TypeError at import. Use `@safe_op(name=\"docconsistency\", mutates=False)`.

---

### Task 5 — Record initial ContractCheck baseline (commit live nvidia-smi snapshot)

**Depends on:** Task 2

**Estimated complexity:** low

**Files in scope:**
- `data/contracts/nvidia-smi-watchloop/2026-XX-XXTXX-XX-XXZ.json`
- `data/contracts/nvidia-smi-watchloop/latest_ref.txt`

**Files (read-only context):**
- `config/arcis_config.yaml`
- `src/tools/contractcheck/core.py`

**Description:**

After T2 lands, record the first live baseline for the `nvidia-smi-watchloop` contract on the operator's machine and commit BOTH files (the timestamped JSON + latest_ref.txt) into the PR.\n\nSteps:\n1. Run `python -m src.tools.contractcheck record nvidia-smi-watchloop`.\n2. Verify the resulting files: `data/contracts/nvidia-smi-watchloop/<timestamp>.json` exists and is well-formed JSON; `data/contracts/nvidia-smi-watchloop/latest_ref.txt` exists and contains just the filename of the new JSON (no trailing newline, no path prefix).\n3. Read the JSON. Confirm `cmd` matches the arcis_config.yaml entry verbatim. Confirm `returncode == 0` (or document the failure mode if nvidia-smi is unavailable on the implementation machine — in that case, this task is SCOPED-OUT and the operator records the baseline manually post-merge).\n4. If parse_ok is False (e.g., live nvidia-smi returns [N/A] for a column), document this in the PR description as the EXPECTED outcome — it's the latent #117 bug surfacing exactly as the spec §4.9 predicts. Do NOT modify any production code to make parse_ok = True (that would be fixing #117 inside #107).\n5. `git add data/contracts/nvidia-smi-watchloop/<timestamp>.json data/contracts/nvidia-smi-watchloop/latest_ref.txt`. Verify `git status` shows them as tracked (NOT ignored).\n\nIf the implementation environment lacks nvidia-smi (e.g., CI runner without GPU): the task is scoped-out for the agent; the operator records on their dev box post-merge. In this case T5 produces NO files but T6's tests still use mocked subprocess output and pass.

**Test strategy:**

Manual verification: file existence + JSON validity + latest_ref.txt content correctness. The eventual `tests/tools/test_contractcheck_integration.py::test_verify_against_committed_baseline` (added in T6) loads this baseline at test time and asserts it parses with the expected schema — that test transitively validates T5's output.

**Scope fence:** Do NOT modify system_metrics.py — the latent [N/A] gap is #117 follow-up. Do NOT pre-edit the JSON to look 'clean' — record whatever the live system produces. Do NOT add other contracts beyond nvidia-smi-watchloop. Do NOT record on a different machine and copy the file — the baseline must come from `record()` on the implementation environment. If nvidia-smi unavailable, scope-out and document in PR.

---

### Task 6 — ContractCheck integration tests (with verify-by-mutation north-star)

**Depends on:** Task 2, Task 5

**Estimated complexity:** medium

**Files in scope:**
- `tests/tools/test_contractcheck_integration.py`

**Files (read-only context):**
- `src/tools/contractcheck/__init__.py`
- `src/tools/contractcheck/__main__.py`
- `src/tools/contractcheck/core.py`
- `data/contracts/nvidia-smi-watchloop/latest_ref.txt`

**Description:**

Write `tests/tools/test_contractcheck_integration.py` per spec §9.2 (ContractCheck section). Filename follows the existing Tier 1+2 convention `test_<toolname>_integration.py` (verified: test_dbquery_integration.py, test_logtail_integration.py, etc.).\n\nKey tests (the full enumeration is in spec §9.2; this is the minimum viable subset):\n1. `test_record_writes_baseline`: mock _subprocess.run returning known stdout; record(); assert JSON file exists + schema correct; assert latest_ref.txt content matches.\n2. `test_record_atomic_write`: simulate mid-write interruption (use a context manager that raises during the tempfile write); assert no partial baseline file is left on disk.\n3. `test_verify_pass`: pre-create a baseline; mock live invocation matching within tolerance; assert verdict='PASS'.\n4. `test_verify_drift_value_mismatch`: pre-create baseline; live exceeds DA1-recalibrated tolerance (e.g., baseline gpu_util_pct=42.0, live=78.0, delta=36 > tolerance 5.0); verdict='DRIFT'; field status='mismatch'. Confirms DA1 value-drift detection works.\n5. **`test_verify_na_north_star`** (THE V0.36.29 NORTH-STAR): pre-create baseline with 5 floats; mock live stdout `'42, 4096, [N/A], 58, 175.30'`; call verify; assert `fields['gpu_vram_total_mb']['status'] == 'shape_change'`, `verdict == 'DRIFT'`. This test is the verify-by-mutation gate for SHAPE drift.\n6. `test_verify_invocation_failed`: subprocess timeout via mock raising TimeoutExpired; verdict='INVOCATION_FAILED'.\n7. `test_baseline_not_found`: no latest_ref; raises BaselineNotFoundError.\n8. `test_contract_not_configured`: contract name absent from config; raises ContractNotConfiguredError.\n9. `test_baseline_corrupt`: malformed baseline JSON; raises BaselineCorruptError.\n10. `test_diff_subcommand`: two pre-created baselines; diff returns expected dict shape.\n11. `test_cli_envelope_record`: subprocess case — invoke `python -m src.tools.contractcheck record nvidia-smi-watchloop --json` with mocked nvidia-smi; assert stdout is JSON with the recorded baseline keys, exit 0.\n12. `test_cli_envelope_error_json`: subprocess case — invoke verify for a nonexistent contract --json; assert stdout is the standard error envelope `{\"error\": {\"type\": \"ContractNotConfiguredError\", ...}}`, exit 1.\n13. `test_verify_against_committed_baseline`: if T5 committed a baseline, load it; assert it parses with the expected schema (this transitively validates T5).\n14. **`test_record_redacts_at_capture_when_configured`** (DA2 — RECORDING-TIME PII SANITIZATION): in a temp config, configure a contract with `at_capture_redact: ['C:\\\\Users\\\\[a-zA-Z]+']`. Mock _subprocess.run returning stdout containing `'pid=12345, path=C:\\Users\\mille\\app.exe, util=42'`. Call record(); read the resulting baseline JSON; assert `baseline['stdout']` contains `'<REDACTED>'` and does NOT contain `'mille'`. Also assert `baseline['normalization_applied']` records that at_capture_redact was applied. This proves PII never lands in committed baselines.\n\nAnti-vacuous-test discipline: before merging, the QA reviewer mutates `verify()` to always return `verdict='PASS'` and confirms `test_verify_na_north_star` AND `test_verify_drift_value_mismatch` BOTH FAIL. If either mutated test still passes, the test is vacuous and must be rewritten. Same anti-vacuous gate for `test_record_redacts_at_capture_when_configured` — temporarily remove the redaction logic from record() and confirm the test FAILS.

**Test strategy:**

Run `pytest tests/tools/test_contractcheck_integration.py -v` — all tests pass. Verify ≥95% line coverage on `src/tools/contractcheck/core.py` via `pytest --cov=src.tools.contractcheck tests/tools/test_contractcheck_integration.py`. Manual mutation verification: temporarily set `verify` to return `verdict='PASS'` always; `test_verify_na_north_star` MUST FAIL; revert.

**Scope fence:** Do NOT modify the contractcheck implementation in this task — only tests. Do NOT skip any test (no pytest.skip / xfail). Do NOT mock at too-high a level (e.g., do not patch contractcheck.core.verify directly — patch _subprocess.run instead so the test exercises real verify logic). Do NOT write tests for the other tools — those are T7.

---

### Task 7 — GitArchaeology + DocConsistency integration tests

**Depends on:** Task 3, Task 4

**Estimated complexity:** medium

**Files in scope:**
- `tests/tools/test_gitarchaeology_integration.py`
- `tests/tools/test_docconsistency_integration.py`

**Files (read-only context):**
- `src/tools/gitarchaeology/`
- `src/tools/docconsistency/`
- `CHANGELOG.md`
- `data/docconsistency-allowlist.yaml`
- `docs/standards/`

**Description:**

Write `tests/tools/test_gitarchaeology_integration.py` and `tests/tools/test_docconsistency_integration.py` per spec §9.2. Filenames follow the existing Tier 1+2 convention `test_<toolname>_integration.py`.\n\nGitArchaeology tests (minimum):\n1. `test_log_basic` (mocked stdout → list-of-dicts).\n2. `test_log_path_filter` (argv contains `-- <path>`).\n3. `test_log_range`.\n4. `test_log_limit` (default 50 + override).\n5. `test_blame_full_file`.\n6. `test_blame_range` (argv contains `-L 10,20`).\n7. `test_blame_invalid_range` (start > end → GitArgError).\n8. `test_show` (parses subject + body + diff).\n9. `test_diff` (argv contains `<ref_a>..<ref_b>`).\n10. `test_rev_list`.\n11. `test_merge_base`.\n12. `test_tag_l`.\n13. `test_git_missing` (`shutil.which('git')` patched to return None → GitMissingError with install hint).\n14. `test_git_invocation_error` (subprocess returns exit 128 with stderr → GitInvocationError; assert stderr is wrapped).\n15. `test_dubious_ownership_hint` (stderr contains the literal phrase 'fatal: detected dubious ownership' → assert the GitInvocationError message includes the `git config --system --add safe.directory` remediation).\n16. **`test_forbidden_op_argparse_rejected`** (subprocess case): invoke `python -m src.tools.gitarchaeology commit`; assert exit code 2, stderr contains `invalid choice: 'commit'`. This regression-locks the structural FORBIDDEN-list enforcement.\n17. `test_cli_envelope_json_log` (subprocess case, --json mode returns valid JSON).\n\n**DA3 parsing-contract tests (NEW):**\n18. **`test_log_subject_with_embedded_tab`**: mock _subprocess.run returning a log line with an embedded tab in the subject (e.g., `sha1\\tauthor\\t2026-05-25\\tfix:\\tactually do the thing`). Assert log() returns 1 dict where subject == 'fix:\\tactually do the thing' (the tab preserved IN the subject — NOT split into a phantom 5th column). Proves DA3 rule 1 (maxsplit) works.\n19. **`test_log_custom_format_requires_columns`**: call log(format='%H%x09%an%x09%ae%x09%s') WITHOUT format_columns kwarg → assert GitArgError is raised BEFORE any git subprocess invocation. Proves DA3 rule 2 (paired kwargs) is enforced.\n20. **`test_log_custom_format_with_columns`**: call log(format='%H%x09%an%x09%ae%x09%s', format_columns=['sha', 'author', 'email', 'subject']) → assert log() returns dicts with 4 keys matching format_columns; embedded tabs in subject preserved (split with maxsplit 3).\n21. **`test_log_parse_failure_raises`**: mock stdout to return a malformed line (e.g., only 2 tabs when 3 expected). Assert log() raises GitParseError with offending_line + expected_columns + op fields populated.\n\n**DA4 size-governance tests (NEW):**\n22. **`test_blame_large_file_requires_range`**: create a temp file with 5001 lines; call blame(file=temp_path) without start_line/end_line. Assert GitArgError is raised BEFORE any git subprocess invocation. Proves DA4 pre-invocation gate.\n23. **`test_blame_large_file_truncates_cleanly`**: create temp file with 5001 lines; call blame(file=temp_path, start_line=1, end_line=10, max_output_bytes=100). Mock _subprocess.run to return 200 bytes of fake blame output. Assert GitOutputTruncatedError raised with partial_output and original_size_bytes populated; partial_output length ≤ 100 (and UTF-8 codepoint-boundary safe — no mid-codepoint truncation).\n24. **`test_show_respects_max_output_bytes`**: call show(sha='deadbeef', max_output_bytes=50); mock stdout returning 5000 bytes; assert GitOutputTruncatedError raised; partial_output is exactly 50 bytes; original_size_bytes == 5000.\n25. **`test_show_default_max_output_bytes`**: call show(sha='deadbeef') with no explicit cap; mock stdout returning 5MB; assert no error (5MB < 10MB default). Mock stdout returning 11MB; assert GitOutputTruncatedError raised.\n26. **`test_cli_max_output_bytes_flag`** (subprocess case): invoke `python -m src.tools.gitarchaeology show <sha> --max-output-bytes 100 --json`; mock stdout (via env-var or fixture) returning >100 bytes; assert exit 1 with structured error envelope `{'error': {'type': 'GitOutputTruncatedError', 'partial_output': ..., 'original_size_bytes': ...}}`.\n\nDocConsistency tests (minimum):\n1. `test_scan_finds_existing_refs_ok` (fixture md with valid ref → no findings).\n2. `test_scan_file_missing` (fixture md with nonexistent file ref → severity file_missing).\n3. `test_scan_line_missing` (fixture md with line >> file length → severity line_missing).\n4. `test_scan_range_uses_start_line` (Pattern B uses start line; start exists → no finding).\n5. `test_scan_allowlist_suppresses` (fixture allowlist YAML masks the finding → empty findings, refs_allowlisted=1).\n6. `test_scan_allowlist_missing_file_is_empty` (allowlist path doesn't exist → no error, empty allowlist).\n7. `test_scan_allowlist_malformed` (invalid YAML → DocConsistencyAllowlistError).\n8. `test_scan_pattern_a_backtick`.\n9. `test_scan_pattern_b_range`.\n10. `test_scan_pattern_c_ignored` (comma-list `vram_manager.py:266,340` NOT matched — proves v2 deferral is explicit).\n11. `test_scan_default_targets_includes_changelog`.\n12. `test_scan_default_targets_includes_docs_standards` (verify docs/standards/boundary-touch-tests.md is picked up by default-scope glob — confirms the docs/standards/ directory existence is exercised).\n13. `test_scan_default_targets_age_filters_docs_audits` (mock os.path.getmtime to return a 100-day-old timestamp → that file excluded; mock to return a 10-day-old timestamp → included).\n14. `test_scan_explicit_target_overrides_age_filter`.\n15. `test_scan_target_missing` (`--target` to nonexistent path → DocConsistencyTargetMissingError).\n16. `test_scan_changelog_md_real_run`: runs against the actual repo CHANGELOG.md; asserts refs_found > 100 (smoke — doesn't assert specific findings, so it's flake-resistant).\n17. `test_cli_envelope_json` (subprocess case).\n\nAnti-vacuous-test discipline applies — QA reviewer verifies each negative test FAILS when the production code is intentionally broken.

**Test strategy:**

Run `pytest tests/tools/test_gitarchaeology_integration.py tests/tools/test_docconsistency_integration.py -v` — all tests pass. Verify ≥95% line coverage on both subpackages via pytest --cov. Manual mutation gates: (a) temporarily delete the FORBIDDEN-list argparse rejection logic and confirm `test_forbidden_op_argparse_rejected` FAILS; revert. (b) DA3 mutation: temporarily change log() parser to use `line.split('\\t')` without maxsplit; confirm `test_log_subject_with_embedded_tab` FAILS; revert. (c) DA3 mutation: temporarily remove the format/format_columns mismatch check; confirm `test_log_custom_format_requires_columns` FAILS; revert. (d) DA4 mutation: temporarily remove the post-invocation size check in _git; confirm `test_show_respects_max_output_bytes` FAILS; revert. (e) DA4 mutation: temporarily remove the pre-invocation 5000-line gate in blame(); confirm `test_blame_large_file_requires_range` FAILS; revert.

**Scope fence:** Do NOT modify gitarchaeology or docconsistency implementations — tests only. Do NOT skip any test. Do NOT couple the gitarchaeology test to actual remote-network operations (all tests use mocked _subprocess.run except a single optional smoke test that runs `git log --limit 1` against the local repo — that's acceptable per the existing #105/#106 pattern). Do NOT write contractcheck tests here — those are T6.

---

### Task 8 — CHANGELOG entry + version bump + final lint sweep

**Depends on:** Task 6, Task 7

**Estimated complexity:** low

**Files in scope:**
- `CHANGELOG.md`
- `src/__init__.py`
- `pyproject.toml`

**Files (read-only context):**
- `src/tools/contractcheck/`
- `src/tools/gitarchaeology/`
- `src/tools/docconsistency/`
- `tests/tools/`

**Description:**

Final integration task.\n\nSteps:\n1. Open `CHANGELOG.md`. Add a new entry at the top under `## [Unreleased]` (or, if operator's convention is per-release, add a new `## [v0.36.65] — 2026-MM-DD — Tier 3 meta-quality tools` section). Follow the existing entry shape (Added / Fixed / Tests / Root cause / Operator action / Open follow-up / References sections per CHANGELOG.md L82-90 precedent). At minimum the entry must include:\n   - Added: ContractCheck v1, GitArchaeology v1 (7 ops), DocConsistency v1 (class (a) only).\n   - References: file:line refs to the new modules (src/tools/contractcheck/core.py, src/tools/gitarchaeology/core.py, src/tools/docconsistency/core.py).\n   - Open follow-up: link to task #117 (Restore [N/A] defense in system_metrics.py nvidia-smi parser) as the latent bug surfaced by ContractCheck's first baseline.\n   - Open follow-up: link to §11.3, §11.4, §11.5 deferrals from the design spec.\n2. Bump the version constant in the canonical version file (likely `src/__init__.py` or `pyproject.toml` — verify and update accordingly). New version: v0.36.65 (re-baselined at impl time per BRIEF — operator may bump if a hotfix lands between design and impl).\n3. Run full repo lint: `ruff check src/tools/contractcheck src/tools/gitarchaeology src/tools/docconsistency tests/tools/` — must be clean.\n4. Run full repo test suite: `pytest tests/tools/ -v` — all tests pass (Tier 1, Tier 2, AND new Tier 3 *_integration.py files).\n5. Run `python -m src.tools.contractcheck record nvidia-smi-watchloop --json` if T5 was scoped-out at the implementation environment; otherwise verify `python -m src.tools.contractcheck verify nvidia-smi-watchloop --json` produces a clean verdict (likely PASS since the baseline was recorded moments ago from the same machine).\n6. Verify `git status` shows: 15 new files + 4 modified files + 2 documentation-touched (CHANGELOG.md + version file). Matches §7 file tree.\n7. Verify the CHANGELOG entry's file:line refs do not themselves trigger DocConsistency findings (run scan after committing the entry).

**Test strategy:**

Run `pytest tests/tools/ -v` — entire tools test suite (Tier 1, 2, 3) passes. Run `ruff check src/tools/` clean. Run `python -m src.tools.docconsistency scan --target CHANGELOG.md --json` — assert the new v0.36.65 entry's refs are all valid (no new findings introduced by the changelog itself).

**Scope fence:** Do NOT modify any tool implementation in this task — only CHANGELOG + version + verification. Do NOT bump version if operator's hotfix workflow has bumped between design and impl — coordinate with operator. Do NOT add new tests. Do NOT modify any pre-Tier-3 CHANGELOG entry. Do NOT touch MASTER.md (the BRIEF and deep report agree that auto-discovery via src/tools/ subpackage layout means no MASTER.md edit is needed).

---

