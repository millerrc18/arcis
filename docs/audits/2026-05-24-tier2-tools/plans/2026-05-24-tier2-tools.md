# Arcis #106 Tier 2 Tools — Implementation Plan

**Spec:** [`docs/audits/2026-05-24-tier2-tools/specs/2026-05-24-tier2-tools-design.md`](../specs/2026-05-24-tier2-tools-design.md)
**Target release:** v0.36.63 (re-baseline at implementation time per #105 lesson)
**Estimated effort:** ~1 day agent work, dual-Opus QA
**Tasks:** 9 across 6 execution batches
**Tier 2 inherits Tier 1 (#105) patterns; introduces FIRST mutating tools.**

---

## Implementation Discipline (read first)

## Implementation Discipline (read first)

Execution (FB4 — re-ordered):
- Batch 1 [1]: Foundation helpers + audit-kind extension + FB1 parametrize/frozenset-equality test update.
- Batch 2 [2, 5, 6, 7]: Config schema extension (Task 2 — FB2 paths.watchdog_heartbeat + bundled _secrets test) runs in parallel with PRComments (depends on Task 1 only), CapabilityRegistryQuery (depends on Task 1 only), TestPatternScan (depends on Task 1 only). These four are independent — no shared file_in_scope, no cross-dependencies.
- Batch 3 [3]: ProcessManager runs alone — depends_on [1, 2] (needs Task 2's cfg.paths.watchdog_heartbeat).
- Batch 4 [4]: HealthProbe runs alone — depends_on [1, 2, 3] (needs ProcessManager.nssm.nssm_status import + cfg.paths.watchdog_heartbeat).
- Batch 5 [8]: CHANGELOG + version bump after all 5 tools land.
- Batch 6 [9]: Pre-merge integration verification.

Six batches honor FB4's intent: Task 3 (ProcessManager → renumbered, was Task 2 in v1) and Task 4 (HealthProbe → renumbered, was Task 3 in v1) each run alone after their dependencies, with maximum parallelism for the independent siblings (Tasks 2/5/6/7 in batch 2).

File-count budget: every task respects ≤4 files_in_scope. Tasks 3 and 4 list core.py + a sub-module + __main__.py + test (4 files); __init__.py is co-created in the same edit as core.py (subpackage convention — Tier-1 inherited; reviewer accepted in v1).

Version baseline: Tier 1 (#105) at v0.36.62; Tier 2 (#106) target v0.36.63. PM RE-BASELINES at implementation time per the operator's #105 lesson — if main has drifted, the bump is current+1.

New/changed contracts in this design:
- T2-DA1: ProcessManager POSITIVE state parsing via _STATE_MAP iteration (7-state vocab, first substring match wins). Task 3 test (a)(b)(c) verify all states correctly distinguished.
- T2-DA2: ProcessManager restart wait-and-verify protocol (30s state poll + 5s log evidence verify). RestartResult dataclass.
- T2-DA3: PID-scoped kill discipline in taskkill.py — NEVER /im, NEVER by name. Task 3 test (k).
- T2-DA4: PRComments PRCommentLeakError pre-flight with detect_secret_in_text. 2-row audit pattern.
- T2-DA5: TestPatternScan rule defaults — vacuous + patch_drift ON; mock_only + side_effect_unreached OPT-IN.
- T2-DA6: CapabilityRegistryQuery v1 PURE registry — dataclasses.asdict(TableDef) directly.
- T2-DA7: HealthProbe staleness defaults 60s/300s/30s in code; --stale-seconds N CLI override.
- T2-DA8: _VALID_RESULTS extended with 'secret_leak_block' + ATOMIC parametrize + frozenset-equality test update (FB1).
- T2-DA9: Anti-pattern sibling-search grep recorded in Task 1 + final grep-assert in Task 9.
- T2-DA10 (FB2 NEW): paths.watchdog_heartbeat config key — explicit, decoupled from db_canonical.parent; consumed by ProcessManager.restart + HealthProbe._check_heartbeat. Task 9 grep-asserts no fallback resolution under src/tools/.
- T2-DA11 (FB3): HealthProbe HARD dependency on #105's src.tools.logtail.tail — no fallback path. Task 4 PRECONDITION verifies import.
- T2-DA12 (FB5): Real-seam smoke tests for ProcessManager (nssm_status) + PRComments (read) with skipif(shutil.which()) gates.
- T2-DA13 (FB6): PRComments documents gh >= 2.0 minimum + auth-failure-as-GhCommandFailedError-with-hint + rate-limit-stderr-passthrough. Task 5 test (h) exercises auth-failure path.
- T2-DA14 (DA1 → 16 design decisions): `design_decisions.json` lists 16 entries (DD-1 through DD-16) — one for each row of spec §8 table. DA1 remediation added DD-11 (sub-module split heuristic), DD-12 (shared _subprocess.run + resolve_exe), DD-13 (registry-import exception), DD-14 (gh stdin pipe + gh >= 2.0), DD-15 (real-seam smoke tests) as full entries with options_considered/rationale/reversibility/downstream_implication. DD-16 added per DA2 (sustained-running flap detection).
- T2-DA15 (DA2): ProcessManager.restart SUSTAINED-RUNNING flap detection. After first RUNNING observation, the poll continues for 3 additional 1s polls; ALL 3 follow-up observations must remain RUNNING. Any non-RUNNING follow-up resets the sustained-window timer. Overall deadline 33s (30s initial + up to 3s sustained). Task 3 adds flap-detection test (canned [RUNNING, STOPPED, RUNNING, RUNNING, RUNNING] sequence) and verifies elapsed_s > 5s AND state=RUNNING AND verified depends on log-evidence check happening AFTER sustained-running confirmation (NOT during the early-transient RUNNING). Task 9 grep-asserts presence of `consecutive_running` or `sustained` in nssm.py. Recorded as DD-16.
- T2-DA16 (DA3): _BODY_SECRET_PATTERNS extended with 5 new known-prefix patterns (ghs_, ghu_, glpat-, sk_live_, pk_live_, JWT 3-segment) + a high-entropy fallback (40+ char base64-like run, labeled 'high_entropy_unknown' in the kind field) that catches AWS secret access keys (40-char base64-shaped) + unknown vendor tokens. detect_secret_in_text now returns a 3-tuple (is_leak, redacted_preview, kind). Task 2 test_secrets.py expanded to 16 cases (was 9). Task 9 grep-asserts presence of the new patterns + high-entropy reference in _secrets.py. DD-4 rationale updated.
- T2-DA17 (DA4): TestPatternScan PatchDriftRule uses PURE-AST symbol resolver — never imports the target module. Implementation: importlib.util.find_spec (import-free) to locate the module's source file → Path.read_text + ast.parse → walk tree.body top-level only collecting names from Assign/AnnAssign/FunctionDef/AsyncFunctionDef/ClassDef/Import/ImportFrom. Symbol resolution checks first segment of dotted name against the collected frozenset. Documented miss: setattr()-injected names + class-attribute chains. Eliminates the side-effect-bomb risk where scanning a test that patches src.api.app.X would trigger load_dotenv/DB connections/FastAPI app instantiation in CI. Task 7 adds dedicated side-effect-safety test (poison-trap monkeypatches on load_dotenv + sqlite3.connect + psycopg2.connect + assert DATABASE_URL env unchanged) using a test file that patches src.api.app.something_important. Task 9 grep-asserts zero `importlib.import_module` and zero `__import__` in src/tools/testpatternscan/. DD-5 rationale updated.

Forbidden-imports baseline (Tier-1 inherited + Tier-2 exception):
- Tier-1 forbidden: from src.config, import src.config, from src.utils.db, import src.utils.db, os.environ.get('DATABASE_URL', os.getenv('DATABASE_URL', load_dotenv, subprocess.run(.*shell=True.
- Tier-2 EXCEPTION: `from src.schema.registry import TABLES` is PERMITTED ONLY in src/tools/capabilityregistry/core.py. Task 9 verifies the exception appears in exactly one file.

Out-of-scope deferral: NONE. If you encounter broken tests / hardcoded values / drifted references / repairable defects WHILE executing the effort, fix INSIDE the effort. Do not write 'Out of scope (pre-existing failures)' in PR body. If a surfaced issue is GENUINELY larger than the effort, STOP + surface via AskUserQuestion.

Dual-Opus QA gate per #98: two independent Opus reviewers verify root-cause / hardening / ripple / noise dimensions at 100% confidence before merge.

---

## Execution Order

**Batch 1:** Task 1

**Batch 2:** Task 2, Task 5, Task 6, Task 7

**Batch 3:** Task 3

**Batch 4:** Task 4

**Batch 5:** Task 8

**Batch 6:** Task 9

---

## Tasks

### Task 1 — Scaffold _secrets + _subprocess helpers, extend _VALID_RESULTS + parametrize coverage, grep verifications

**Estimated complexity:** low

**Files in scope:**
- `src/tools/_secrets.py`
- `src/tools/_subprocess.py`
- `src/tools/_execution_log.py`
- `tests/tools/test_execution_log.py`

**Files (read-only context):**
- `src/tools/_safety.py`
- `src/scheduler/watch.py`
- `src/scheduler/ollama_watchdog.py`
- `src/schema/registry.py`
- `scripts/statusline.py`

**Description:**

Create src/tools/_secrets.py per spec §2.3 (DA3-REVISED) — detect_secret_in_text(body) returns a 3-tuple (is_leak, redacted_preview, kind). High-confidence patterns: ghp_, github_pat_, gho_, ghs_ (DA3 — server-to-server), ghu_ (DA3 — user-to-server), glpat- (DA3 — GitLab), sk-, sk_live_ (DA3 — Stripe), pk_live_ (DA3 — Stripe), xox[abprs]-, JWT 3-segment shape eyJ.eyJ.X (DA3), password=/api_key=/secret=/token= shapes, Authorization: Bearer, PEM private key blocks, AWS AKIA. Low-confidence high-entropy fallback (DA3): 40+ char alphanumeric/base64-like run that did NOT match a known prefix — labeled 'high_entropy_unknown' in the kind field; catches AWS secret access keys (40-char base64-shaped) + generic vendor tokens. Both kinds block equally; the kind label is purely audit-row metadata. Create src/tools/_subprocess.py (resolve_exe(name) with @lru_cache and NssmMissingError/GhMissingError; GhMissingError message includes gh >= 2.0 requirement per spec §3.3; run(args, *, timeout=10, check=False, input_data=None) wrapper enforcing capture_output=True+text=True+encoding='utf-8'+NEVER shell=True). MODIFY src/tools/_execution_log.py:50-56 — extend _VALID_RESULTS frozenset to add 'secret_leak_block' (single-line addition). MODIFY tests/tools/test_execution_log.py:192-194 (FB1) — extend parametrize list to include 'secret_leak_block' AND add a new assertion that imports _VALID_RESULTS and asserts equality against the literal frozenset({'success','dry_run','safety_window_block','prod_guard_block','error','secret_leak_block'}) — catches future drift if the source extends but the test isn't updated. GREP VERIFICATIONS (record results as comments in _subprocess.py header for downstream tasks): (a) `grep -n 'def safety_window' src/tools/_safety.py` confirms safety_window decorator implementation lines (needed for ProcessManager.restart contract); (b) `grep -rn '_sc_query_running\|sc query' src/scheduler/` confirms current anti-pattern siblings (record file:line list — if MORE than 2 known instances at watch.py:130-147 and watch.py:1161-1163 exist, surface via AskUserQuestion); (c) `grep -rn 'TABLES\b' src/schema/registry.py` confirms 80-count + TABLES dict location; (d) `grep -n 'watchdog\.txt\|watchdog_heartbeat' src/scheduler/watch.py scripts/statusline.py` confirms the cwd-relative write site (watch.py:1722-1734) + statusline discovery walk (statusline.py:38-55) cited in spec §2.2 + §3.1. All file opens encoding='utf-8'. Six-line module header on every new file.

**Test strategy:**

test_execution_log.py parametrize at ~line 193 is extended to 6 entries: ['success','dry_run','safety_window_block','prod_guard_block','error','secret_leak_block']. New assertion (same or sibling test): from src.tools._execution_log import _VALID_RESULTS; assert _VALID_RESULTS == frozenset({'success','dry_run','safety_window_block','prod_guard_block','error','secret_leak_block'}) — this fails RED immediately if a future change adds a 7th kind without updating the test (the FB1 drift-detector). Existing parametrize test verifies write_event accepts each of the 6 result kinds without raising. Verify-by-mutation: remove 'secret_leak_block' from the parametrize list → write_event-accepts test silently still passes (only 5 kinds get tested) — the frozenset-equality assertion is what catches this drift. Smoke: `python -c 'from src.tools._execution_log import _VALID_RESULTS; assert len(_VALID_RESULTS)==6'` returns exit 0. Grep results recorded in _subprocess.py module header consumed by Task 3 (safety_window line cite) and Task 4 (anti-pattern citations).

**Scope fence:** Do NOT modify _safety.py, _db.py, _cli_envelope.py (frozen #104+#105). Do NOT implement any tool — tools are Tasks 3-7. Do NOT add value-driven scan to sanitize_params — _secrets.detect_secret_in_text is the new helper. Do NOT remove or rename existing _VALID_RESULTS members — only ADD 'secret_leak_block'. Do NOT add tests/tools/test_secrets.py to THIS task — _secrets.py implementation tests are deferred to Task 5 (PRComments owns _secrets via dependency) or moved to Task 2 if scope allows; Task 1 ships _secrets.py module + the 6-entry _VALID_RESULTS test only.

---

### Task 2 — Foundation config extension: paths.watchdog_heartbeat (FB2)

**Depends on:** Task 1

**Estimated complexity:** low

**Files in scope:**
- `src/tools/_config.py`
- `config/arcis_config.yaml`
- `tests/tools/test_secrets.py`

**Files (read-only context):**
- `src/tools/_secrets.py`
- `src/scheduler/watch.py`
- `scripts/statusline.py`

**Description:**

MODIFY src/tools/_config.py — extend PathsConfig pydantic model with new field: `watchdog_heartbeat: Path = Path('C:/arcis/halcyon-lab/data/watchdog.txt')`. Place the field after existing path fields (alphabetical or grouped per current PathsConfig ordering — read the file first to match convention). MODIFY config/arcis_config.yaml — add `watchdog_heartbeat: 'C:/arcis/halcyon-lab/data/watchdog.txt'` under the existing `paths:` block. Path value matches the actual NSSM AppDirectory + scheduler write location (operator-confirmed). Create tests/tools/test_secrets.py (positive/negative samples per regex pattern in _secrets.py from Task 1 — DA3 EXTENDED PATTERN COVERAGE). test_secrets asserts 16 terminal states across the 15 known-prefix patterns + high-entropy fallback + clean-body baselines: (a) ghp_<20chars> → leak + kind='known_prefix'; (b) github_pat_<20+chars> → leak + kind='known_prefix'; (c) sk-<20+chars> → leak + kind='known_prefix'; (d) 'password=hunter2' → leak + kind='known_prefix'; (e) 'Authorization: Bearer xyz...' → leak + kind='known_prefix'; (f) '-----BEGIN RSA PRIVATE KEY-----' → leak + kind='known_prefix'; (g) AKIA<16 uppercase> → leak + kind='known_prefix'; **NEW DA3 CASES:** (h) ghs_<36+chars> → leak + kind='known_prefix' (GitHub server-to-server); (i) ghu_<36+chars> → leak + kind='known_prefix' (GitHub user-to-server); (j) glpat-<20+chars> → leak + kind='known_prefix' (GitLab); (k) sk_live_<24+chars> → leak + kind='known_prefix' (Stripe live secret); (l) pk_live_<24+chars> → leak + kind='known_prefix' (Stripe live publishable); (m) JWT three-segment 'eyJabc.eyJxyz.signaturepart12345678901' → leak + kind='known_prefix'; (n) 40-char base64-like 'wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY' (AWS secret access key shape, no known prefix) → leak + kind='high_entropy_unknown'; **CLEAN BASELINES:** (o) plain text 'looks good to me' → not leak + kind='none' + body unchanged; (p) empty string → not leak + kind='none'. Verify-by-mutation: comment out the ghs_ regex → (h) test fails; comment out the high-entropy fallback → (n) test fails. RATIONALE FOR THIS TASK'S EXISTENCE: FB2 explicitly identified the original spec's `cfg.paths.db_canonical.parent / 'watchdog.txt'` as wrong (would resolve to C:/arcis/data/watchdog.txt; actual location is C:/arcis/halcyon-lab/data/watchdog.txt). Option (a) — explicit config key — chosen per FB2 remediation. This task isolates the foundation-extension surface so the config schema change has its own commit/PR boundary for git-bisect + reviewer visibility. All file opens encoding='utf-8'. Six-line module header on test_secrets.py.

**Test strategy:**

test_secrets.py covers 16 terminal states per DA3-extended pattern coverage: (a) ghp_<20chars> → leak,kind='known_prefix'; (b) github_pat_<20+chars> → leak,known_prefix; (c) sk-<20+chars> → leak,known_prefix; (d) 'password=hunter2' → leak,known_prefix; (e) 'Authorization: Bearer xyz...' → leak,known_prefix; (f) PEM private key block → leak,known_prefix; (g) AKIA<16upper> → leak,known_prefix; (h) ghs_<36+chars> → leak,known_prefix (DA3); (i) ghu_<36+chars> → leak,known_prefix (DA3); (j) glpat-<20+chars> → leak,known_prefix (DA3); (k) sk_live_<24+chars> → leak,known_prefix (DA3); (l) pk_live_<24+chars> → leak,known_prefix (DA3); (m) JWT 'eyJabc.eyJxyz.signaturepart12345678901' → leak,known_prefix (DA3); (n) 'wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY' (AWS-secret-shape, no known prefix) → leak,kind='high_entropy_unknown' (DA3); (o) 'looks good to me' → not leak,kind='none',body unchanged; (p) empty string → not leak,kind='none'. Verify-by-mutation per case: comment out the ghp_ regex → (a) fails; comment out ghs_ → (h) fails; comment out the high-entropy fallback → (n) fails. Config validation smoke: `python -c 'from src.tools._config import load_arcis_config; cfg = load_arcis_config(); assert cfg.paths.watchdog_heartbeat.name == "watchdog.txt"; print(cfg.paths.watchdog_heartbeat)'` returns exit 0 + prints the absolute path. YAML round-trip: `python -c 'import yaml; data = yaml.safe_load(open("config/arcis_config.yaml", encoding="utf-8")); assert "watchdog_heartbeat" in data["paths"]'`. Pydantic model coercion: ensure the string from YAML coerces to Path object (pydantic does this automatically for Path-typed fields). If src/tools/test_config.py exists (read-only check), no changes are needed — pydantic adds the field non-destructively (any existing config without the key uses the default).

**Scope fence:** Do NOT change the WRITE site at src/scheduler/watch.py:1722-1734 — out of scope; behavior continues unchanged. Do NOT change scripts/statusline.py's _resolve_data_root discovery — out of scope; statusline can opt-in later. Do NOT add field validators beyond pydantic's default Path coercion. Do NOT add new path fields beyond watchdog_heartbeat. Do NOT touch other arcis_config.yaml sections (services, ports, safety_windows). Do NOT implement any tool — tools are Tasks 3-7.

---

### Task 3 — Implement ProcessManager (5-file subpackage + integration test)

**Depends on:** Task 1, Task 2

**Estimated complexity:** high

**Files in scope:**
- `src/tools/processmanager/core.py`
- `src/tools/processmanager/nssm.py`
- `src/tools/processmanager/taskkill.py`
- `tests/tools/test_processmanager_integration.py`

**Files (read-only context):**
- `src/tools/_safety.py`
- `src/tools/_subprocess.py`
- `src/tools/_config.py`
- `src/scheduler/ollama_watchdog.py`
- `scripts/archive_bootcamp_2026_04_24.py`

**Description:**

Create src/tools/processmanager/{__init__.py, __main__.py, core.py, nssm.py, taskkill.py} and tests/tools/test_processmanager_integration.py. nssm.py exports nssm_status(service) -> ServiceState using POSITIVE state-map (per spec §3.1 — iterate _STATE_MAP in order, first substring match wins; SERVICE_RUNNING/STARTING/STOPPING/CONTINUE_PENDING/PAUSE_PENDING/PAUSED/STOPPED in that order); nssm_start/stop/restart(service) -> CompletedProcess via _subprocess.run([resolve_exe('nssm'), verb, service], timeout=15). core.py exports status/start/stop/restart with decorator stacks per spec §2.4: status @safe_op(mutates=False); start/stop @safe_op(mutates=True); restart @safe_op(mutates=True)→@safety_window('no_restart_overnight'). Service-alias resolver: accepts 'watch'/'watch_loop'/'ArcisWatchLoop' (cfg.services.watch_loop), 'ollama'/'ollama_watchdog'/'ArcisOllamaWatchdog', 'dashboard'/'ArcisDashboard'. Unknown alias → UnknownServiceError. **WAIT-AND-VERIFY RESTART PROTOCOL (DA2-REVISED SUSTAINED-RUNNING):** Record start_monotonic + start_walltime; nssm_restart returns; poll nssm_status every 1.0s up to 30s INITIAL deadline. On the FIRST RUNNING observation, enter SUSTAINED-WINDOW: continue polling 3 more times at 1.0s intervals (additional 3s); ALL 3 follow-up observations must remain RUNNING. If ANY follow-up observation is NOT RUNNING (STARTING/STOPPING/STOPPED/UNKNOWN/PAUSED), the sustained-window timer RESETS — the poll re-enters the initial-deadline phase. OVERALL deadline = 33s (30s initial + up to 3s sustained-window). On 3-consecutive-RUNNING success, exit poll loop with elapsed_s = time.monotonic() - start_monotonic + proceed to step 4. Implementation hint: use a counter `consecutive_running = 0` that increments on each RUNNING observation and resets to 0 on any non-RUNNING observation; exit when counter >= 3 (1 initial + 2 follow-up) OR when initial deadline + sustained-window budget exhausted. On 33s overall deadline without 3-consecutive-RUNNING → RestartResult(restarted=True, verified=False, elapsed_s=~33.0, log_evidence=None, state=<last observation>). After sustained-running confirmed, verify log_evidence within 5s window — for watch_loop, mtime check uses `cfg.paths.watchdog_heartbeat` (NEW from Task 2 per FB2/DD-10) — NOT `cfg.paths.db_canonical.parent / 'watchdog.txt'` — for dashboard use arcis-dashboard.log mtime; for ollama_watchdog use arcis-ollama-watchdog.log mtime; mtime must be >= start_walltime; return RestartResult(restarted, verified, elapsed_s, log_evidence, state). taskkill.py exports kill_pid(pid) using ollama_watchdog.py:240-260 pattern (taskkill /f /t /pid → PowerShell Stop-Process escalation) — PID-SCOPED ONLY, NEVER /im, NEVER by name. __main__.py uses _cli_envelope.run_cli; argparse with positional verb + positional service + --confirm/--emergency/--json. Markdown rendering per spec §3.1 examples. ≤300 LOC for core.py; ≤250 LOC nssm.py; ≤150 LOC taskkill.py. NOTE: __init__.py + __main__.py + nssm.py + taskkill.py are ALL created here but only 4 file slots are listed (init + main + core + tests); nssm.py and taskkill.py are co-created in the same task — count them under the parent processmanager/ directory for budget purposes (Task 1/2 established the 5-file subpackage pattern is parameterizable; reviewer accepted in original Task 2).

**Test strategy:**

test_processmanager_integration.py: tmp_path audit log + monkeypatch subprocess seam returning canned nssm outputs + monkeypatch time.monotonic + monkeypatch cfg.paths.watchdog_heartbeat to a tmp_path file (so log-evidence check is deterministic). Assert 9+ terminal states: (a) status returns RUNNING for canned 'SERVICE_RUNNING' stdout + 'success' event; (b) status returns STARTING for 'SERVICE_START_PENDING' (POSITIVE PARSING — verifies SERVICE_STOP_PENDING is not misread as RUNNING by negative-style check); (c) status returns STOPPING for 'SERVICE_STOP_PENDING'; (d) restart from RUNNING-canned (sequence: [RUNNING, RUNNING, RUNNING, RUNNING] — initial + 3 sustained-window observations all RUNNING) + watchdog_heartbeat file mtime advanced post-restart → RestartResult(restarted=True, verified=True, elapsed_s<33, log_evidence=<watchdog_heartbeat path>, state=RUNNING) + 'success' event; (e) restart with stuck STARTING canned (never reaches RUNNING) → RestartResult(restarted=True, verified=False, elapsed_s=~33, state=STARTING) — monkeypatched time.monotonic to fast-forward; (f) restart with sustained-RUNNING canned but log mtime unchanged → RestartResult(verified=False, log_evidence=None); (g) restart with safety_window now_et=21:45 ET → SafetyWindowError raised + 'safety_window_block' event + zero duplicate 'error' events (decorator contract); (h) NssmMissingError raised when monkeypatched shutil.which returns None + 'error' event; (i) UnknownServiceError on unknown alias 'foo' + 'error' event; (j) CLI subprocess `python -m src.tools.processmanager status ArcisWatchLoop --json` writes valid JSON envelope on forced nssm-missing failure + exit 1; (k) taskkill discipline: kill_pid(99999) → captures returncode + verifies _subprocess.run was called with ['taskkill','/f','/t','/pid','99999'] list-form (NEVER /im). **DA2 FLAP-DETECTION TEST:** (l) canned status sequence [RUNNING, STOPPED, RUNNING, RUNNING, RUNNING] + watchdog_heartbeat mtime ADVANCED EARLY (between observation 1 and 2 — simulating dying process writing last heartbeat) → RestartResult(restarted=True, verified=<depends on whether sustained-window completed within 33s overall deadline>) + state=RUNNING. CRITICAL ASSERTION: the FIRST RUNNING is NOT enough to exit poll; the STOPPED observation MUST reset the sustained-window counter (consecutive_running goes back to 0); only after 3 consecutive RUNNINGs starting from observation 3 does the loop exit. Test verifies: (l.1) elapsed_s > 5 (so sustained-window logic was actually exercised, not a single-observation exit); (l.2) if sustained-window completes within 33s → state=RUNNING; (l.3) verified depends on whether the LATE log-evidence check (after sustained-running, in step 4) sees a mtime > start_walltime — for this test, monkeypatch the heartbeat mtime to be advanced ONLY ONCE early (between obs 1 and 2) and NOT after sustained-running → verified=False (the false-early-mtime is correctly rejected because the log-evidence check happens AFTER sustained-running confirmation, not during). FB5 REAL-SEAM SMOKE: (m) `test_processmanager_real_nssm_smoke` — `@pytest.mark.skipif(shutil.which('nssm') is None, reason='nssm.exe not on PATH')` — call `nssm_status('ArcisWatchLoop')` against real binary, assert returned `state.value` is in the 8-value set {'RUNNING','STOPPED','STARTING','STOPPING','PAUSED','PAUSE_PENDING','CONTINUE_PENDING','UNKNOWN'}. Read-only contract probe. Verify-by-mutation: replace POSITIVE _STATE_MAP iteration with NEGATIVE 'SERVICE_STOPPED not in stdout' → (b) fails; remove the safety_window decorator → (g) fails; change taskkill arg list to include '/im' → (k) fails; hardcode watchdog path as cfg.paths.db_canonical.parent / 'watchdog.txt' → (d) fails; **DA2 FLAP-DETECTION MUTATION:** revert to single-RUNNING exit (consecutive_running counter removed; loop exits on first RUNNING) → (l) fails because the flap's first transient RUNNING wrongly exits the poll + the (l.1) elapsed_s > 5 assertion is violated (would be < 2s); ALSO if log-evidence check runs DURING poll rather than after sustained-running → (l.3) verified=True wrongly.

**Scope fence:** Do NOT implement ProcessManager.kill — deferred to Tier 3. Do NOT add @prod_guard (no DSN). Do NOT inherit _sc_query_running pattern (watch.py:130-147) — use nssm_status via _subprocess.run with timeout. Do NOT silently swallow FileNotFoundError (archive_bootcamp_2026_04_24.py:166 anti-pattern) — raise NssmMissingError. Do NOT use NEGATIVE state parsing (archive_bootcamp:157-169 anti-pattern). Do NOT use /im or kill-by-name in taskkill.py. Do NOT resolve watchdog path via db_canonical.parent — use cfg.paths.watchdog_heartbeat (DD-10 / Task 2). Do NOT use cwd-relative Path('data/watchdog.txt') — absolute config-driven path only. Do NOT exceed LOC budgets.

---

### Task 4 — Implement HealthProbe (4-file subpackage + integration test)

**Depends on:** Task 1, Task 2, Task 3

**Estimated complexity:** high

**Files in scope:**
- `src/tools/healthprobe/core.py`
- `src/tools/healthprobe/checks.py`
- `src/tools/healthprobe/__main__.py`
- `tests/tools/test_healthprobe_integration.py`

**Files (read-only context):**
- `src/tools/_safety.py`
- `src/tools/_subprocess.py`
- `src/tools/_config.py`
- `src/tools/processmanager/__init__.py`
- `src/tools/logtail/`
- `scripts/statusline.py`

**Description:**

PRECONDITION (FB3): At the start of this task, verify `from src.tools.logtail import tail` is importable. If ImportError (because #105 has not merged on main yet), STOP the task and surface via AskUserQuestion. No fallback file-tail path — mandatory dependency. Create src/tools/healthprobe/{__init__.py, __main__.py, core.py, checks.py} and tests/tools/test_healthprobe_integration.py. checks.py exports 4 probe functions per spec §3.2: _check_service_state(service) imports nssm_status from src.tools.processmanager.nssm; _check_port(port, host='127.0.0.1') uses socket.socket(AF_INET, SOCK_STREAM) + sock.settimeout(0.5) + sock.connect_ex returning bool (True iff connect_ex==0); _check_heartbeat(path, max_age_s) subsumes statusline.py:72-84 pattern (read ISO timestamp; tz=None defaults to UTC; (now_utc - last_utc).total_seconds() < max_age_s); returns (fresh: bool, reason: str | None) — reasons: 'file_missing' / 'parse_error' / f'age={N}s>threshold={M}s' / None when fresh. Heartbeat PATH SOURCE: resolved from `cfg.paths.watchdog_heartbeat` (NEW from Task 2 per FB2/DD-10) — NO _resolve_data_root() discovery walk; _check_recent_errors(log_path, window_minutes=15) imports tail from src.tools.logtail (Tier-1 → Tier-2 inheritance per §1.1) — count lines containing ERROR/CRITICAL substring after lines whose timestamp is within window. core.py exports check(*, services=None, stale_seconds=None) with @safe_op(mutates=False). Default services = ['ArcisWatchLoop','ArcisOllamaWatchdog','ArcisDashboard'] from cfg.services. Default stale_seconds per spec §3.2 table: 60/300/30 for watch_loop/dashboard/ollama_watchdog. Verdict aggregation per spec §3.2 matrix. __main__.py uses _cli_envelope.run_cli; argparse with --services NAME[,NAME] (comma-split) + --stale-seconds N + --json. core.py ≤250 LOC; checks.py ≤300 LOC.

**Test strategy:**

test_healthprobe_integration.py: tmp_path audit log + tmp_path heartbeat file (monkeypatch cfg.paths.watchdog_heartbeat to tmp_path) + monkeypatch socket.connect_ex + monkeypatch nssm_status. Assert 7+ terminal states: (a) all healthy (canned RUNNING + fresh heartbeat + connect_ex=0 + 0 errors) → overall='OK' + per-service verdicts=OK + 'success' event; (b) watch_loop heartbeat 120s old (>60s threshold) but RUNNING → service verdict=DEGRADED + heartbeat_reason='age=120s>threshold=60s' + overall=DEGRADED; (c) ollama_watchdog connect_ex=ETIMEDOUT (port 11434 not listening) but RUNNING → verdict=DEGRADED + port_listening=False; (d) dashboard nssm_status=STOPPED → verdict=DOWN + overall=DOWN; (e) heartbeat file does not exist → fresh=False + reason='file_missing' + verdict=DEGRADED; (f) heartbeat file with garbage content 'not-a-timestamp' → reason='parse_error' + verdict=DEGRADED; (g) --stale-seconds 600 override → previously-stale heartbeat now passes (age=120s<600s) → verdict=OK; (h) CLI subprocess --json on forced cfg load failure (monkeypatch load_arcis_config to raise) writes envelope to stdout + exit 1. Verify-by-mutation: remove _check_heartbeat tz=UTC fallback → tz-naive timestamps fail wrongly → (a) fails; remove worst-of overall aggregation → (d) overall='OK' wrongly → (d) fails; hardcode heartbeat path as 'data/watchdog.txt' (cwd-relative) → (a)(b)(e)(f) fail in CI (cwd ≠ NSSM AppDirectory). Boundary-touch coverage per spec §6.1.

**Scope fence:** Do NOT implement port-binding tests — connect_ex is non-binding by design. Do NOT replicate statusline.py's cwd-relative Path('data/watchdog.txt') OR statusline._resolve_data_root() discovery — resolve via cfg.paths.watchdog_heartbeat. Do NOT use ctypes.OpenProcess — HealthProbe relies on nssm state + heartbeat freshness. Do NOT add DB-row checks (orthogonal to src/api/routes/health.py per scout). Do NOT inherit _sc_query_running anti-pattern. Do NOT add a logtail-fallback path — mandatory #105 dependency.

---

### Task 5 — Implement PRComments (3-file subpackage + integration test)

**Depends on:** Task 1, Task 2

**Estimated complexity:** medium

**Files in scope:**
- `src/tools/prcomments/__init__.py`
- `src/tools/prcomments/__main__.py`
- `src/tools/prcomments/core.py`
- `tests/tools/test_prcomments_integration.py`

**Files (read-only context):**
- `src/tools/_safety.py`
- `src/tools/_secrets.py`
- `src/tools/_subprocess.py`
- `src/tools/_execution_log.py`

**Description:**

Create src/tools/prcomments/{__init__.py, __main__.py, core.py} and tests/tools/test_prcomments_integration.py. core.py exports read(pr, *, repo=None) -> list[PRComment] decorated with @safe_op(mutates=False), and post(pr, body, *, confirm=False, repo=None) -> dict decorated with @safe_op(mutates=True) (NO @safety_window per spec §2.4). Read: _subprocess.run([resolve_exe('gh'),'pr','view',str(pr),'--json','comments'] + (['-R', repo] if repo else []), timeout=30); parse stdout as JSON; map comments[] → PRComment(author=c['author']['login'], body=c['body'], created_at=c['createdAt'], url=c['url']). Post: BEFORE invoking gh, call detect_secret_in_text(body) from src.tools._secrets — if is_leak=True, directly call write_event(log_path=..., tool_name='prcomments', params={'pr': pr, 'body_redacted': redacted_preview[:500]}, result='secret_leak_block', duration_ms=0) THEN raise PRCommentLeakError(redacted_preview) — @safe_op's exception handler will write the additional 'error' event (the 2-row pattern per spec §5). If no leak: _subprocess.run([resolve_exe('gh'),'pr','comment',str(pr),'--body-file','-'] + (['-R', repo] if repo else []), input_data=body, timeout=30); parse stdout for comment URL. PRECONDITIONS (FB6 — spec §3.3): minimum gh CLI version >= 2.0 required for --body-file - stdin (NOT pre-checked; rely on gh-side error surfaced as GhCommandFailedError); auth precondition NOT pre-flighted (rely on gh's own auth error, surface as GhCommandFailedError with hint='Run `gh auth status` then `gh auth login` if needed.' embedded in exception message); secondary rate-limit NOT retried (surface gh's stderr verbatim in GhCommandFailedError, caller decides retry policy). Errors per spec §3.3: PRCommentsError base, PRCommentLeakError, GhMissingError (re-export from _subprocess), GhCommandFailedError (returncode!=0, signature: __init__(returncode, stderr, *, hint=None)), GhJsonParseError. __main__.py uses _cli_envelope.run_cli; argparse with positional verb (read/post) + positional pr (int) + --body STRING (mutex with --body-file PATH) + --repo + --confirm + --json. ≤350 LOC for core.py.

**Test strategy:**

test_prcomments_integration.py: tmp_path audit log + monkeypatch _subprocess.run + monkeypatch resolve_exe. Assert 9+ terminal states: (a) read returns parsed list[PRComment] for canned `gh pr view --json comments` JSON output + 'success' event; (b) post-success with clean body → 'success' event + dict with comment_url; (c) POST-LEAK-BLOCK (CRITICAL): post(pr=1, body='here is my token ghp_abc1234567890123456789') → PRCommentLeakError raised + 'secret_leak_block' event written with body_redacted containing '***REDACTED***' + 'error' event also written (2-row pattern per spec §5) + verify subprocess.run was NEVER called with 'comment' (the gh subprocess MUST be intercepted before invocation); (d) post-leak-block with sk-<token> → same 2-row pattern; (e) post-leak-block with 'password=hunter2' → same; (f) GhMissingError when monkeypatched resolve_exe raises → 'error' event + error message contains gh >= 2.0 hint; (g) GhCommandFailedError when subprocess returncode=1 (generic failure) → 'error' event + stderr captured; (h) FB6 AUTH-FAILURE: monkeypatch _subprocess.run to return CompletedProcess(returncode=1, stderr='error: authentication required, run `gh auth login` to authenticate') → GhCommandFailedError raised + exception message includes hint='Run `gh auth status` then `gh auth login` if needed.' + 'error' event written + verify hint is in str(exc); (i) GhJsonParseError when stdout is not valid JSON for read path → 'error' event; (j) CLI subprocess --json on forced gh-missing writes envelope to stdout + exit 1; (k) PRComments.post with body via --body-file stdin pipe — verify subprocess.run was called with args containing '--body-file', '-' AND input_data=body (NOT --body <string>). FB5 REAL-SEAM SMOKE: (l) `test_prcomments_real_gh_smoke` — `@pytest.mark.skipif(shutil.which('gh') is None, reason='gh.exe not on PATH')` + secondary skipif on `subprocess.run(['gh','auth','status'], capture_output=True).returncode != 0` (skip with reason='gh not authenticated') — call `read(pr=<known-merged-PR-from-this-repo, e.g. 1086>, repo='millerrc18/halcyon-lab')`, assert `isinstance(result, list)` and (if non-empty) `isinstance(result[0], PRComment)` with the 4 expected dataclass fields. Read-only contract probe — verifies real gh JSON shape still maps to our PRComment. Does NOT call post(). Verify-by-mutation: remove the pre-flight detect_secret_in_text call → leak tests (c)(d)(e) pass body to gh + 'secret_leak_block' event never written → tests fail on missing audit row; replace '--body-file' '-' with '--body' body → CLI smuggling test (k) fails on missing stdin pipe arg; drop the hint='Run...' kwarg from GhCommandFailedError → auth-failure test (h) fails on missing hint in exc message.

**Scope fence:** Do NOT use --body <string> (shell-escaping risk for arbitrary content). Do NOT raise SafetyError — PRCommentLeakError is a tool-internal check, not a decorator guard (SafetyError reserved for #104 cross-cutting primitives). Do NOT add @safety_window — GitHub writes are low-risk + rate-limited. Do NOT add gh log streaming / gh run view (those are CIInvestigate). Do NOT call GitHub REST directly. Do NOT skip the leak pre-flight even with --confirm/--emergency. Do NOT pre-flight `gh --version` or `gh auth status` — rely on gh's own errors per FB6. Do NOT implement retry/backoff for rate limits — surface verbatim per FB6.

---

### Task 6 — Implement CapabilityRegistryQuery (3-file subpackage + integration test)

**Depends on:** Task 1

**Estimated complexity:** low

**Files in scope:**
- `src/tools/capabilityregistry/__init__.py`
- `src/tools/capabilityregistry/__main__.py`
- `src/tools/capabilityregistry/core.py`
- `tests/tools/test_capabilityregistry_integration.py`

**Files (read-only context):**
- `src/tools/_safety.py`
- `src/schema/registry.py`

**Description:**

Create src/tools/capabilityregistry/{__init__.py, __main__.py, core.py} and tests/tools/test_capabilityregistry_integration.py. core.py exports tables(*, sync_only=False) -> dict[str, dict] and table(name) -> dict, both decorated with @safe_op(mutates=False). Import `from src.schema.registry import TABLES` is PERMITTED per spec §2.2 EXCEPTION — document the exception in core.py module header. tables() returns {name: dataclasses.asdict(t) for name, t in TABLES.items()}; if sync_only=True, filter t.sync_to_postgres=True. table(name): raise CapabilityRegistryError if name not in TABLES (include the registered count in the error message). __main__.py uses _cli_envelope.run_cli; argparse with [--table NAME] [--sync-only] [--json]; if --table given, call table(name); else call tables(sync_only=...). Markdown rendering per spec §3.4 examples (single-table = full columns + indexes + sync block; all-tables = compact table with name/description/columns_count/sync). ≤200 LOC for core.py.

**Test strategy:**

test_capabilityregistry_integration.py: tmp_path audit log + use real TABLES import (registry is module-level data, no runtime apparatus). Assert 6+ terminal states: (a) tables() returns dict with len==80 + every value has 11 expected keys (name/description/columns/primary_key/indexes/foreign_keys/sync_to_postgres/sync_mode/sync_time_column/sync_pk/sync_conflict_col/sync_reconcile per spec §3.4) + 'success' event; (b) tables(sync_only=True) returns subset where all values have sync_to_postgres=True + count >0; (c) table('shadow_trades') returns dict with name='shadow_trades' + columns list + 'success' event; (d) table('does_not_exist') raises CapabilityRegistryError + error message contains the 80-count + 'error' event; (e) tables() output is JSON-serializable via json.dumps (dataclasses.asdict produces nested-dict output) — assert json.dumps(tables()) does not raise; (f) primary_key field can be str OR list[str] (composite) — find one composite-key table in TABLES and assert isinstance(table['primary_key'], list); (g) CLI subprocess `python -m src.tools.capabilityregistry --table shadow_trades --json` writes valid JSON + exit 0; (h) CLI --table unknown_name --json writes error envelope + exit 1. Verify-by-mutation: replace dataclasses.asdict(t) with t.__dict__ → nested ColumnDef objects no longer serialize → json.dumps test (e) fails; remove the CapabilityRegistryError raise → unknown-table test (d) returns None/error elsewhere → test fails.

**Scope fence:** Do NOT compose with DBQuery (live row counts) — explicitly deferred to Tier 3 ContractCheck v2 per brief. Do NOT add write/update operations — registry is the SoT; updates go through schema/registry.py editorially. Do NOT cache results — registry is module-level frozen data already. Do NOT use Pydantic v1 .dict() — use dataclasses.asdict directly (registry uses @dataclass, not pydantic). Do NOT add filter args beyond --table and --sync-only.

---

### Task 7 — Implement TestPatternScan (4-file subpackage + integration test)

**Depends on:** Task 1

**Estimated complexity:** high

**Files in scope:**
- `src/tools/testpatternscan/core.py`
- `src/tools/testpatternscan/rules.py`
- `src/tools/testpatternscan/__main__.py`
- `tests/tools/test_testpatternscan_integration.py`

**Files (read-only context):**
- `src/tools/_safety.py`
- `tests/api/test_local_routes_auth_coverage.py`
- `docs/standards/boundary-touch-tests.md`

**Description:**

Create src/tools/testpatternscan/{__init__.py, __main__.py, core.py, rules.py} and tests/tools/test_testpatternscan_integration.py. rules.py exports 4 Rule subclasses, each with detect(tree: ast.Module, source: str, filepath: str) -> list[Finding]: VacuousRule (default ON, precision=HIGH per spec §3.5 — detects functions where @patch or Mock() is in scope but the body contains no .assert_* method call); **PatchDriftRule (default ON, precision=HIGH, DA4 PURE-AST RESOLVER)** — detects @patch('module.symbol') where the target module's SOURCE FILE (located via importlib.util.find_spec — IMPORT-FREE per Python contract) cannot be ast-parsed OR the symbol is not in the module's TOP-LEVEL AST node table. NEVER call importlib.import_module + NEVER use getattr-walk — these would EXECUTE the target module's top-level code (load_dotenv, DB connections, FastAPI app instantiation), turning the static scanner into a side-effect bomb that could attempt prod-DB connections in CI when scanning a test that patches src.api.app.X. Helper `_module_top_level_names(module_name) -> frozenset[str] | None` (cached via @lru_cache(maxsize=512)): (1) importlib.util.find_spec(module_name); if None or .origin None or .origin=='built-in' → return None; (2) source_path = Path(spec.origin); if suffix != '.py' → return None; (3) ast.parse(source_path.read_text(encoding='utf-8')) — on SyntaxError/UnicodeDecodeError return None; (4) walk tree.body TOP-LEVEL ONLY — DO NOT recurse into FunctionDef/ClassDef bodies — collect names from Assign targets (handle Tuple-targets), AnnAssign target, FunctionDef/AsyncFunctionDef/ClassDef .name, Import aliases (`alias.asname or alias.name.split('.')[0]`), ImportFrom aliases (`alias.asname or alias.name`); return frozenset. Helper `_resolve_symbol_in_module(module_name, symbol) -> bool`: split symbol on '.' and check if first segment in the cached frozenset. PatchDriftRule.detect: for each @patch('module.symbol') decorator argument seen, call _resolve_symbol_in_module; on None-from-helper → flag detail='module not importable / source not parseable'; on False-from-resolution → flag detail=f'symbol {symbol!r} not in top-level AST of {module_name}'. Documented miss: setattr()-injected names + class-attribute chains (e.g. Foo.bar where Foo is defined but bar comes from __init__). MockOnlyRule (OPT-IN, precision=MEDIUM — detects test functions whose ONLY assertions are mock_x.assert_*(...)/mock_x.call_args, with no asserts on real SUT return values); SideEffectUnreachedRule (OPT-IN, precision=LOW — detects mock.side_effect = Exception(...) where the mock is also given return_value=... and the test path appears to take the return_value branch). Use ast.walk pattern from tests/api/test_local_routes_auth_coverage.py:66-124. core.py exports scan(*, path=None, kinds=None) -> list[Finding] with @safe_op(mutates=False). Default path = repo_root / 'tests'. Default kinds=['vacuous','patch_drift']. Discovery: Path(path).rglob('test_*.py') + filter not p.name.startswith('_'). Parse each file via ast.parse(source) — on SyntaxError, skip with WARNING (don't crash entire scan). __main__.py uses _cli_envelope.run_cli; argparse with [--path DIR] [--kinds K[,K...]] [--json]. Markdown rendering per spec §3.5 — group findings by rule kind with confidence summary. core.py ≤250 LOC; rules.py ≤400 LOC. **CRITICAL: zero imports of `importlib.import_module` and zero `__import__` calls anywhere in src/tools/testpatternscan/ — verified by Task 9 grep-assert.**

**Test strategy:**

test_testpatternscan_integration.py: tmp_path audit log + inline test-source strings per rule (per test_local_routes_auth_coverage.py:251-281 pattern). Assert 11+ terminal states (one positive + one negative per rule + opt-in coverage + DA4 side-effect-safety): (a) VACUOUS POSITIVE — inline source with `@patch('foo.bar'); def test_x(mock_bar): mock_bar.return_value = 42; result = sut(); assert result == 42` where mock_bar is set up but never .assert_*-ed → 1 vacuous finding; (b) VACUOUS NEGATIVE — same shape but body adds `mock_bar.assert_called_once()` → 0 vacuous findings; (c) PATCH-DRIFT POSITIVE — `@patch('src.foo.gone_symbol')` where src.foo exists but gone_symbol does NOT → 1 patch_drift finding with detail='symbol gone_symbol not in top-level AST of src.foo'; (d) PATCH-DRIFT NEGATIVE — `@patch('src.tools._safety.safe_op')` (real symbol present in src/tools/_safety.py top-level) → 0 patch_drift findings; (e) MOCK-ONLY POSITIVE (opt-in: kinds=['mock_only']) → 1 mock_only finding; (f) MOCK-ONLY OFF BY DEFAULT → 0 findings; (g) SIDE-EFFECT-UNREACHED POSITIVE (opt-in: kinds=['side_effect_unreached']) → 1 finding; (h) SIDE-EFFECT-UNREACHED OFF BY DEFAULT → 0 findings; (i) UNKNOWN KIND — kinds=['nonexistent_rule'] → TestPatternScanError + 'error' event; (j) SYNTAX ERROR IN TARGET FILE → scan skips file with WARNING + continues + 0 findings raised; (k) CLI subprocess `python -m src.tools.testpatternscan --kinds nonexistent --json` writes error envelope + exit 1; (l) CLI subprocess `python -m src.tools.testpatternscan --path <tmp_path_with_positive_samples> --json` returns JSON list of findings + exit 0. **DA4 SIDE-EFFECT-SAFETY TEST:** (m) scan a TEST file whose source contains `@patch('src.api.app.something_important')`; BEFORE invoking scan, install poison-traps via monkeypatch: (m.1) monkeypatch `dotenv.load_dotenv` to raise RuntimeError if called; (m.2) monkeypatch `sqlite3.connect` to raise RuntimeError if called; (m.3) monkeypatch `psycopg2.connect` to raise RuntimeError if called (or skip if not installed in test env); (m.4) record initial value of `os.environ.get('DATABASE_URL')` and assert it's unchanged after scan. Run `scan(path=<tmp_path with the test file>, kinds=['patch_drift'])`. Assert the scan completes without raising AND no poison-trap fired AND DATABASE_URL env is unchanged. This proves the PURE-AST resolver never imports src.api.app (which would trigger load_dotenv, FastAPI app instantiation, DB connection openers). Verify-by-mutation for (m): replace _module_top_level_names with a body that calls `importlib.import_module(module_name)` → test (m) fails because importing src.api.app triggers load_dotenv (which is monkeypatched to raise). Verify-by-mutation for other tests: comment out the assert_* search in VacuousRule → (b) fails; replace importlib.util.find_spec with always-True stub in PatchDriftRule → (c) fails (would no longer report symbol-not-found). Precision/recall per spec §3.5 documented in module docstring + rule class docstrings.

**Scope fence:** Do NOT implement schema-source-of-truth drift detection (Tier 3 ContractCheck). Do NOT implement decorator-composition rule (covered by test_safe_op_integration.py at runtime). Do NOT replace ast.walk with NodeVisitor (precedent at test_local_routes_auth_coverage.py uses ast.walk). Do NOT crash entire scan on a single SyntaxError — skip + WARNING. Do NOT add LSP / type-inference — pure AST static. Do NOT exceed LOC budgets.

---

### Task 8 — CHANGELOG + version bump (RE-BASELINE at implementation time)

**Depends on:** Task 3, Task 4, Task 5, Task 6, Task 7

**Estimated complexity:** low

**Files in scope:**
- `CHANGELOG.md`
- `src/version.py`
- `tests/test_version.py`

**Description:**

Append v0.36.63 entry to CHANGELOG.md per spec §9. RE-BASELINE protocol: when this task begins, run `python -c 'from src.version import VERSION; print(VERSION)'` to confirm current main version. If current != v0.36.62 (Tier 1's target), the bump is current+1. UPDATE src/version.py VERSION constant atomically with tests/test_version.py:18 _EXPECTED_VERSION literal — partial updates RED tests/test_version.py. CHANGELOG calls out: five tools (ProcessManager + HealthProbe + PRComments + CapabilityRegistryQuery + TestPatternScan); new _secrets.py + _subprocess.py helpers; _VALID_RESULTS extension for 'secret_leak_block' + atomic test_execution_log.py parametrize update (FB1); NEW paths.watchdog_heartbeat config key (FB2 / DD-10); ProcessManager wait-and-verify protocol (30s+5s); HealthProbe staleness defaults (60/300/30); PRComments stdin-pipe + leak pre-flight + gh >= 2.0 requirement + auth-error-with-hint + rate-limit-passthrough (FB6); CapabilityRegistryQuery pure-registry v1; TestPatternScan rule defaults + precision/recall; real-seam smoke tests for ProcessManager + PRComments (FB5); anti-patterns explicitly NOT inherited (watch.py:130-147, watch.py:1161-1163, archive_bootcamp_2026_04_24.py:157-169, watch.py:1722-1734+statusline.py:38-55 cwd-relative-heartbeat-write+discovery-read); dual-Opus QA. Open() uses encoding='utf-8'.

**Test strategy:**

pytest tests/test_version.py — must PASS after both src/version.py VERSION and tests/test_version.py:18 _EXPECTED_VERSION are bumped to the re-baselined version atomically. RED tests/test_version.py = non-atomic bump (FIX by completing the partial update in the same commit, do NOT skip the test). Smoke: `python -c 'from src.version import VERSION; print(VERSION)'` prints the re-baselined version; `python -c 'from src.api.app import app'` does not raise.

**Scope fence:** Do NOT modify other CHANGELOG sections. Do NOT add Removed/Deprecated. Patch bump only. Do NOT touch pyproject.toml (does not exist at repo root). Do NOT split version + test-literal across commits — atomic. Do NOT hardcode v0.36.63 if main has drifted — RE-BASELINE per the lesson from #105.

---

### Task 9 — Pre-merge integration verification

**Depends on:** Task 8

**Estimated complexity:** low

**Files (read-only context):**
- `src/tools/`
- `tests/tools/`
- `config/arcis_config.yaml`

**Description:**

Run full Tier-2 test surface: pytest tests/tools/ -v. Confirm all new Tier-2 tests pass + keystone test_safe_op_integration.py still passes + Tier-1 tests still pass + tests/test_version.py passes. Confirm test_execution_log.py FB1 parametrize + frozenset-equality assertion both pass. FORBIDDEN-IMPORTS GREP across src/tools/processmanager/, healthprobe/, prcomments/, capabilityregistry/, testpatternscan/ + their test files for spec §2.2 patterns — any match outside docstrings/comments fails the task. SPECIAL CASE: `from src.schema.registry import TABLES` inside src/tools/capabilityregistry/core.py is the ALLOWED EXCEPTION — verify it appears ONLY in that one file and nowhere else under src/tools/. SHELL=TRUE GREP: `grep -rn 'shell=True' src/tools/` must return zero matches. POSITIVE-STATE-MAP GREP: `grep -n 'SERVICE_RUNNING\|SERVICE_STOPPED\|SERVICE_STARTING\|SERVICE_STOP_PENDING' src/tools/processmanager/` must show the 7-state vocabulary present in nssm.py. ANTI-PATTERN GREP: `grep -rn '_sc_query_running\|sc query' src/tools/` must return zero matches. PID-DISCIPLINE GREP: `grep -n 'taskkill.*\/im\|Stop-Process -Name' src/tools/processmanager/` must return zero matches. WATCHDOG-PATH GREP: `grep -rn 'db_canonical.*watchdog\|Path.*data/watchdog' src/tools/` must return zero matches (heartbeat path comes ONLY from cfg.paths.watchdog_heartbeat per DD-10). **DA4 IMPORT-SIDE-EFFECT GREP:** `grep -rn 'importlib\.import_module\|__import__' src/tools/testpatternscan/` MUST return zero matches (the PURE-AST resolver per DD-5/DA4 forbids module imports — the scanner must NEVER execute target module top-level code; if either pattern is found, that confirms a regression where PatchDriftRule's getattr-walk was reintroduced and the scanner could trigger load_dotenv/DB connections/FastAPI app instantiation when scanning tests). **DA2 SUSTAINED-WINDOW GREP:** `grep -n 'consecutive_running\|sustained' src/tools/processmanager/nssm.py` must show at least 1 reference (sustained-window counter implementation present); if zero matches, that confirms flap-detection per DD-16 was not implemented. **DA3 EXTENDED-PATTERN GREP:** `grep -n 'ghs_\|ghu_\|glpat-\|sk_live_\|eyJ' src/tools/_secrets.py` must show at least 5 matches (the DA3-added patterns are present); `grep -n '_HIGH_ENTROPY_PATTERN\|high_entropy_unknown' src/tools/_secrets.py` must show at least 1 match. CONFIG-KEY PRESENCE: `python -c 'from src.tools._config import load_arcis_config; print(load_arcis_config().paths.watchdog_heartbeat)'` exits 0 + prints the absolute path. CLI SMOKE: `python -m src.tools.<name> --help` exits 0 for all 5 Tier-2 tools. CLI ENVELOPE SMOKE: force a failure under --json for each Tier-2 tool + assert stdout is valid JSON matching {error: {type, message, tool}} schema + exit 1. DUAL-OPUS QA gate per #98.

**Test strategy:**

This task IS the test — pytest + 10 greps + 1 config smoke + 5 CLI smokes + 5 envelope smokes. No new files. If any check fails, return to originating task (do not patch in this task — preserves task isolation for git bisect). Specifically: pytest tests/tools/ failure → originating tool task; tests/test_version.py failure → Task 8; forbidden-imports match → originating tool task (or Task 1/2 if in _secrets.py/_subprocess.py/_config.py); shell=True grep match → originating tool task; _sc_query_running grep match → originating tool task (anti-pattern re-introduced); /im or Stop-Process -Name grep match → Task 3 (PID-discipline regression); db_canonical/watchdog or Path('data/watchdog') match → originating tool task (DD-10 regression); **DA4 importlib.import_module/__import__ match in src/tools/testpatternscan/ → Task 7 (PURE-AST resolver regression — scanner now triggers module-import side effects); DA2 missing consecutive_running/sustained reference in src/tools/processmanager/nssm.py → Task 3 (sustained-window flap-detection per DD-16 not implemented — RestartResult.verified can produce false positives during NSSM AppRestartDelay flaps); DA3 missing ghs_/ghu_/glpat-/sk_live_/eyJ patterns in src/tools/_secrets.py → Task 1 (extended pattern list per DD-4 not implemented — known false-negative class for live token shapes); DA3 missing _HIGH_ENTROPY_PATTERN reference → Task 1 (high-entropy fallback for AWS secret keys + generic vendor tokens not implemented);** config-key absent → Task 2 (FB2 regression); CLI --help nonzero exit → originating tool task; CLI envelope smoke failure → originating tool task. Document the originating-task remediation in the failure report.

**Scope fence:** Do NOT modify any source files. Do NOT skip failing tests. Do NOT add new tests. On failure, document originating task + stop.

---

