# Arcis #105 Tier 1 Tools — Implementation Plan

**Spec:** [`docs/audits/2026-05-24-tier1-tools/specs/2026-05-24-tier1-tools-design.md`](../specs/2026-05-24-tier1-tools-design.md)
**Target release:** v0.36.62
**Estimated effort:** ~1 day agent work, dual-Opus QA
**Tasks:** 9 across 5 execution batches

---

## Implementation Discipline (read first)

Execution: Task 1 first (foundation: _db helper + _cli_envelope helper + parity-test handling + decorator-contract grep verify of _safety.py — the grep result is recorded as a comment in _db.py and consumed by Tasks 2/6's decorator-contract test assertions). Tasks 2-6 PARALLEL (each isolated subpackage + integration test, no inter-tool dependencies). Task 7 sequences after Task 6 (TradingState CLI + render builds on TradingState core/queries from Task 6). Task 8 (CHANGELOG + version bump) sequences after all tool tasks. Task 9 final verification.

File-count budget: every task respects ≤4 files_in_scope (Task 6 = 4, Task 7 = 3, others ≤4).

Schema-drift discipline: shadow_trades has NO `opened_at` (use `actual_entry_time` aliased AS `entry_time`). audit_reports has NO `overall_verdict` (use `overall_assessment` verbatim). Task 9 grep-asserts both substrings absent.

Version baseline: foundation #104 at v0.36.57; mainline v0.36.61; target v0.36.62. tests/test_version.py:18 literal bump MUST be atomic with src/version.py.

New/changed contracts in this revision (REV2):
- DA1: CIInvestigate atomic-write (tempfile + os.fsync + os.replace) + corrupt-cache self-heal (JSONDecodeError → unlink + refetch). Task 4 tests include concurrent-writers + corrupt-recovery cases.
- DA2: TradingState 3-query snapshot consistency via REPEATABLE READ on single connection. Task 6 test includes concurrent-writer-mid-snapshot case. _db.py supports isolation_level kwarg (Task 1).
- DA3: CIInvestigate cache invalidates on rerun via lightweight updatedAt head-check. Task 4 test includes rerun-invalidation case.
- DA4: DBQuery uses server-side named cursor + fetchmany(limit+1) + truncated flag. NO LIMIT-appending to user SQL. jsonb-row warning in docstring/help. Task 2 test includes limit-truncation case.
- DA5: LogTail open-time semantics + fstat post-check + LogTailError('file rotated/truncated mid-read; retry'). Task 3 test simulates rotation via monkey-patched os.fstat.
- DA6: Shared _cli_envelope.run_cli helper (Task 1). All tools use it via __main__.py. Every tool's test asserts envelope schema under --json forced failure.
- DA7: Decorator contract documented in spec §4.7. Task 1 grep-verifies _safety.py classification BEFORE Tasks 2/6 dispatch — the grep result is the citation that Tasks 2/6's decorator-contract test assertions reference. Tests assert 'prod_guard_block' as terminal state with zero duplicate 'error' events.
- DA8: TradingState sub-modules — core (≤300) / queries / render (≤200). Task 6 = core+queries+__init__+test. Task 7 = __main__+render+CLI test. Sub-module pattern documented in §4.8 for Tier-2 inheritance.

Out-of-scope deferral: NONE. If you encounter broken tests / hardcoded values / drifted references / repairable defects WHILE executing the effort, fix INSIDE the effort. Do not write 'Out of scope (pre-existing failures)' in PR body. Two recent failure-mode examples (PR #1172 / #92) — both stayed RED. If a surfaced issue is GENUINELY larger than the effort, STOP + surface via AskUserQuestion.

---

## Execution Order

**Batch 1:** Task 1

**Batch 2:** Task 2, Task 3, Task 4, Task 5, Task 6

**Batch 3:** Task 7

**Batch 4:** Task 8

**Batch 5:** Task 9

---

## Tasks

### Task 1 — Scaffold _db helper, _cli_envelope helper, parity test (augment-if-present), decorator-contract verify

**Files in scope:**
- `src/tools/_db.py`
- `src/tools/_cli_envelope.py`
- `tests/tools/test_db_helper.py`
- `tests/tools/test_prod_signature_parity.py`

**Description:**

Add src/tools/_db.py (thin psycopg2 context-manager helper supporting read_only, isolation_level='REPEATABLE READ', named_cursor='<name>', and timeout kwargs; uses conn.set_session for both readonly + isolation_level; NO src.config / src.utils.db coupling). Add src/tools/_cli_envelope.py (shared run_cli helper: invokes fn, on exception with json_mode=True prints {'error': {'type','message','tool'}} to stdout + sys.exit(1); without json_mode re-raises for Python default traceback). Add tests/tools/test_db_helper.py (real 127.0.0.1:5434: RealDictCursor default, read_only=True triggers ReadOnlySqlTransaction on INSERT, isolation_level='REPEATABLE READ' verifiable via SHOW transaction_isolation, named_cursor yields server-side cursor, timeout=1 raises OperationalError). Handle parity test: grep -rn 'prod_dsn_signatures' tests/tools/ — expected hit is tests/tools/test_config.py:88-99. If present + asserts set equality, document in _db.py module header (Tests: tests/tools/test_db_helper.py, tests/tools/test_config.py for parity) — DO NOT create a duplicate file. If absent or weaker, create tests/tools/test_prod_signature_parity.py. Decorator-contract grep-verify (DA7): run `grep -n 'isinstance.*SafetyError\|SafetyError' src/tools/_safety.py` and confirm @safe_op's exception handler skips writing a duplicate 'error' event for SafetyError subclasses (re-raises clean). Record the verified line numbers in a comment in src/tools/_db.py module header — this becomes the citation for Tasks 2 and 6's decorator-contract test assertions. All file opens use encoding='utf-8'. Six-line module header on every new file.

**Test strategy:**

test_db_helper.py: real 127.0.0.1:5434 — (a) RealDictCursor returns dict rows; (b) read_only=True triggers ReadOnlySqlTransaction on subsequent INSERT; (c) isolation_level='REPEATABLE READ' verified via cur.execute('SHOW transaction_isolation') returning 'repeatable read'; (d) named_cursor='stream_test' yields cursor with .name attribute populated; (e) timeout=1 against 127.0.0.1:1 raises psycopg2.OperationalError within 1.5s wall clock. _cli_envelope tested indirectly via each tool's CLI subprocess test (Tasks 2-7). Parity test: if grep shows test_config.py:88-99 already asserts the equality, no new file created — comment in _db.py header cites the existing locus + asserts (via mutation) breaking one signature in test_config breaks that test. If new file created: imports both sides; verify-by-mutation comment names which line breaks if signatures drift. Decorator-contract grep result is recorded in a 'Decorator contract verified at:' comment in src/tools/_db.py and consumed by Tasks 2/6.

**Scope fence:** Do NOT modify src/tools/_config.py, _safety.py, _execution_log.py. Do NOT modify src/simulation/lifecycle/prod_guard.py. Do NOT modify tests/tools/test_config.py. Do NOT build a connection pool — _db.py is a single contextmanager function. Do NOT add CLI logic outside _cli_envelope. Do NOT implement any tool — tools are Tasks 2-7.

---

### Task 2 — Implement DBQuery tool (subpackage + integration test)

**Depends on:** Task 1

**Files in scope:**
- `src/tools/dbquery/__init__.py`
- `src/tools/dbquery/__main__.py`
- `src/tools/dbquery/core.py`
- `tests/tools/test_dbquery_integration.py`

**Description:**

Create src/tools/dbquery/{__init__.py, __main__.py, core.py} and tests/tools/test_dbquery_integration.py. core.py exports query(sql, *, dsn=None, limit=1000) decorated with @safe_op(name='dbquery', mutates=False) (OUTER) and @prod_guard(dsn_param='dsn') (INNER) — order per spec §4.7. Two-layer read-only: regex r'^\s*(SELECT|WITH)\s' after stripping leading -- comments raises WriteNotPermittedError; post-connect SET TRANSACTION READ ONLY via _db.pg_connect(read_only=True). LIMIT semantics (DA4): use named_cursor='dbquery_stream' for server-side streaming; cursor.itersize = limit + 1; cursor.fetchmany(limit + 1); if len(rows) > limit, set truncated=True + drop sentinel. Do NOT append LIMIT to user SQL. Document jsonb-row warning in CLI --help text + module docstring. Default dsn = cfg.pg.test_dsn. __main__.py uses _cli_envelope.run_cli to handle --json error envelope (DA6); argparse with positional sql + --limit/--json/--dsn. Markdown output: column-aligned table + footer '(N rows, truncated=True/False)'. Module header on every file. All opens encoding='utf-8'. Forbidden-imports grep clean.

**Test strategy:**

test_dbquery_integration.py: real 127.0.0.1:5434, tmp_path log, fixture creates a 100-row table. Assert 6+ terminal states: (a) SELECT returns expected rows + 'success' event; (b) WITH cte returns expected + 'success'; (c) INSERT raises WriteNotPermittedError + 'error' event (NOT 'prod_guard_block'); (d) malformed SQL raises DBQueryError + 'error' event; (e) production DSN signature triggers 'prod_guard_block' event AND no duplicate 'error' event (decorator contract §4.7); (f) limit=10 returns exactly 10 rows with truncated=True in markdown footer (DA4); (g) CLI subprocess `python -m src.tools.dbquery 'INSERT ...' --json` writes envelope {'error': {'type': 'WriteNotPermittedError', ...}} to stdout + exit code 1 (DA6). Verify-by-mutation: comment out the regex check → INSERT test (c) fails; comment out the cursor.fetchmany(limit+1) → truncated test (f) fails.

**Scope fence:** Do NOT use src/utils/db.py (forbidden). Do NOT use sqlparse. Do NOT add a write path / transaction commit. Do NOT add caching. Do NOT append LIMIT to user SQL — use fetchmany. Do NOT load entire result set with fetchall(). ≤500 LOC for core.py.

---

### Task 3 — Implement LogTail tool (subpackage + integration test)

**Depends on:** Task 1

**Files in scope:**
- `src/tools/logtail/__init__.py`
- `src/tools/logtail/__main__.py`
- `src/tools/logtail/core.py`
- `tests/tools/test_logtail_integration.py`

**Description:**

Create src/tools/logtail/{__init__.py, __main__.py, core.py} and tests/tools/test_logtail_integration.py. core.py exports tail(*, lines=100, log_path=None, level=None, grep=None) -> list[str] decorated with @safe_op(name='logtail', mutates=False). Multi-line entry grouping regex: ^\d{4}-\d{2}-\d{2} \d{2}:\d{2}:\d{2}[,.]\d+ \[[^\]]+\] (DEBUG|INFO|WARNING|ERROR|CRITICAL):. Reads file backwards in 64KB chunks for large files; whole-file for <64KB. ROTATION HANDLING (DA5): on open, record initial_size = os.fstat(f.fileno()).st_size; after backward read completes, fstat again — if final_size < initial_size, raise LogTailError('file rotated/truncated mid-read; retry'). Document open-time semantics in module docstring per spec §3.2. Default log_path = cfg.paths.logs_runtime / 'arcis.log'. level filter respects hierarchy. grep is case-sensitive substring on joined entry text. __main__.py uses _cli_envelope.run_cli (DA6); argparse with --lines --level --grep --log-path --json. Markdown: blank-line-separated entries + footer '(N entries, M filtered)'. All opens encoding='utf-8', errors='replace'.

**Test strategy:**

test_logtail_integration.py: tmp_path log + tmp_path target log. Write synthesized file: 3 single-line INFO + 1 multi-line ERROR (with traceback continuation). Assert: (a) lines=10 returns 4 entries (groups traceback); (b) level='ERROR' returns 1 entry; (c) grep='executor' returns matching entries; (d) FileNotFoundError on missing path raises LogTailError + 'error' event; (e) empty file returns []; (f) ROTATION-MID-READ (DA5): create a 200KB synthetic log file → tail() running on the file → mid-read, monkey-patch `os.fstat` to return a shrunk size on the second invocation (final_size < initial_size) → assert LogTailError('file rotated/truncated mid-read; retry') is raised + 'error' event recorded; (g) CLI subprocess `python -m src.tools.logtail --log-path <missing> --json` writes envelope {'error': {'type': 'LogTailError', ...}} to stdout + exit 1 (DA6). Verify-by-mutation: remove entry-start regex → multi-line traceback test (a) fails; remove fstat post-check → rotation test (f) fails.

**Scope fence:** Do NOT add --ctx-key structured-context filter (Tier 2). Do NOT inherit src/main.py:319 CWD-relative default. Do NOT read other log files. Do NOT poll for rotation — single-pass read with fstat post-check is the contract. ≤500 LOC.

---

### Task 4 — Implement CIInvestigate tool (subpackage + integration test)

**Depends on:** Task 1

**Files in scope:**
- `src/tools/ciinvestigate/__init__.py`
- `src/tools/ciinvestigate/__main__.py`
- `src/tools/ciinvestigate/core.py`
- `tests/tools/test_ciinvestigate_integration.py`

**Description:**

Create src/tools/ciinvestigate/{__init__.py, __main__.py, core.py} and tests/tools/test_ciinvestigate_integration.py. core.py exports investigate(run_id, *, repo=None, cache_dir=None) -> dict decorated with @safe_op(name='ciinvestigate', mutates=False). Cache convention: data/cache/ci-investigate/<run_id>.json. CACHE FLOW (DA3 — REVISED): (1) if cache file does NOT exist → full fetch via gh run view --json conclusion,status,displayTitle,headBranch,headSha,createdAt,updatedAt,jobs; if conclusion truthy, atomic-write cache; return. (2) if cache exists → on json.JSONDecodeError, log warning + os.unlink(cache_path) + fall through to full fetch (DA1 recovery contract). On successful cache load, run lightweight HEAD-CHECK: gh run view --json status,conclusion,updatedAt. If head.updatedAt > cached.updatedAt OR head.status != 'completed', re-fetch full payload + atomic-rewrite cache (only if completed); else return cached. (3) --no-cache skips step 2 entirely. ATOMIC WRITE (DA1): write to <run_id>.json.tmp in same dir → f.flush() → os.fsync(f.fileno()) → os.replace(tmp_path, cache_path). subprocess.run(['gh','run','view',str(run_id),'--json',...] + (['-R', repo] if repo else []), capture_output=True, text=True, encoding='utf-8', timeout=30, check=False). Non-zero exit / JSON parse error from gh / 'could not find any workflows' → CIInvestigateError. __main__.py uses _cli_envelope.run_cli (DA6); argparse with positional run_id + --repo/--json/--no-cache. Markdown: header (overall conclusion) + per-job table + failed-step preview.

**Test strategy:**

test_ciinvestigate_integration.py: tmp_path cache_dir + tmp_path log. Gate real-gh tests on shutil.which('gh') (pytest.skip if absent). Assert: (a) real gh call against a known completed Arcis run returns dict with conclusion + writes cache + 'success' event; (b) second call hits cache (no new subprocess; monkey-patched subprocess.run records call count) + 'success'; (c) in-progress payload (monkey-patched subprocess.run returning conclusion=None) does NOT write cache; (d) gh-not-on-PATH raises CIInvestigateError + 'error'; (e) --no-cache forces re-fetch (mtime increment); (f) CONCURRENT-WRITERS (DA1): spawn 2 threads/subprocesses calling investigate(same_run_id) with a 50ms artificial subprocess.run delay → after both complete, assert cache file is valid JSON + no .json.tmp residue + cache content matches payload; (g) CORRUPT-CACHE RECOVERY (DA1): pre-write '{"conclusion": "success"' (truncated, invalid JSON) to cache file → call investigate(run_id) → assert: cache file content replaced with valid full payload + WARNING log line mentioning 'corrupt cache' + os.unlink was called on the corrupt file (intermediate state); (h) RERUN-INVALIDATION (DA3): pre-write valid cached payload with updatedAt=T0 conclusion='failure'; monkey-patch subprocess.run so head-check returns updatedAt=T1>T0 conclusion='success' and full-fetch returns the success body; call investigate(run_id) → assert returned dict has conclusion='success' AND cache file on disk now has the success body (atomic-replace happened); (i) CLI subprocess --json on gh-not-found writes envelope to stdout + exit 1 (DA6). Verify-by-mutation: remove conclusion-truthy check → in-progress test (c) fails; replace os.replace with naive open(w) → concurrent test (f) sees partial file; remove JSONDecodeError except → corrupt test (g) raises instead of self-heals; remove updatedAt head-check → rerun test (h) returns stale failure.

**Scope fence:** Do NOT add log-streaming / job-log download (Tier 2). Do NOT call gh REST directly. Do NOT add concurrency primitives in core.py — the atomic-write contract is the concurrency story. Do NOT auto-detect repo from git remote. ≤500 LOC.

---

### Task 5 — Implement SymbolFind tool (subpackage + integration test)

**Depends on:** Task 1

**Files in scope:**
- `src/tools/symbolfind/__init__.py`
- `src/tools/symbolfind/__main__.py`
- `src/tools/symbolfind/core.py`
- `tests/tools/test_symbolfind_integration.py`

**Description:**

Create src/tools/symbolfind/{__init__.py, __main__.py, core.py} and tests/tools/test_symbolfind_integration.py. core.py exports find(symbol, *, kind='any', path=None) -> list[dict] decorated with @safe_op(name='symbolfind', mutates=False). Patterns: kind='def' uses union of ^\s*(def|class)\s+<symbol>\b and ^\s*<symbol>\s*= (module-level constant); kind='use' uses \b<symbol>\b minus def/class overlaps; kind='any' is union dedup-ed by (file,line). subprocess.run(['rg','--json','--type','py', pattern, str(path)], capture_output=True, text=True, encoding='utf-8', timeout=30, check=False). Parse rg --json line-by-line; collect type=='match' events. Output rows: {file, line, col, kind, snippet}. rg not on PATH → SymbolFindError with remediation hint (winget install BurntSushi.ripgrep.MSVC / scoop install ripgrep / choco install ripgrep). __main__.py uses _cli_envelope.run_cli (DA6); argparse with positional symbol + --kind/--path/--json. Markdown: 'file:line | kind | snippet' table.

**Test strategy:**

test_symbolfind_integration.py: tmp_path log + tmp_path rg root. Write 2 small .py files with known symbols. Assert: (a) find('Foo', kind='def') returns class def + 'success'; (b) find('Foo', kind='use') returns import + reference, excludes class line; (c) find('Foo', kind='any') returns union dedup-ed by (file,line); (d) rg-not-on-PATH raises SymbolFindError with remediation hint + 'error'; (e) symbol-not-found returns [] + 'success'; (f) CLI subprocess --json on rg-not-found writes envelope to stdout + exit 1 (DA6). Verify-by-mutation: break (file,line) dedup → kind='any' sees duplicates → test (c) fails.

**Scope fence:** Do NOT use AST parsing (Tier 2 separate effort). Do NOT pure-Python fallback when rg missing — fail fast (operator decision). Do NOT call-graph. ≤500 LOC.

---

### Task 6 — Implement TradingState core + queries (Python API + integration test)

**Depends on:** Task 1

**Files in scope:**
- `src/tools/tradingstate/__init__.py`
- `src/tools/tradingstate/core.py`
- `src/tools/tradingstate/queries.py`
- `tests/tools/test_tradingstate_integration.py`

**Description:**

Create src/tools/tradingstate/{__init__.py, core.py, queries.py} and tests/tools/test_tradingstate_integration.py. queries.py exports SQL constants (OPEN_POSITIONS_PG, RECENT_AUDIT_PG, GPU_METRICS_PG + SQLite variants — initially identical, single source for forking if syntax diverges). core.py exports state(*, dsn=None, sqlite_path=None) -> dict decorated with @safe_op(name='tradingstate', mutates=False) (OUTER) and @prod_guard(dsn_param='dsn') (INNER) — order per §4.7. SNAPSHOT CONSISTENCY (DA2): all 3 queries execute against a SINGLE connection opened with pg_connect(dsn, read_only=True, isolation_level='REPEATABLE READ') — guarantees consistent point-in-time view across queries even with concurrent watch-loop writes. Query 1: shadow_trades LEFT JOIN recommendations (alias actual_entry_time AS entry_time per F3/decision 10). Query 2: audit_reports ORDER BY created_at DESC LIMIT 1; output key overall_assessment verbatim (no alias, decision 11). Compute stale = (now - created_at) > 36h. Query 3: schedule_metrics WHERE metric_date = CURRENT_DATE → pivot in Python: metrics = {row['metric_name']: row['metric_value'] for row in rows}; ollama_ok = bool(metrics['gpu_health_ollama_ok']) if 'gpu_health_ollama_ok' in metrics else None (None for missing, NOT False). SQLite fallback on psycopg2.OperationalError: sqlite3.connect(cfg.paths.db_canonical, timeout=5); same queries; data_source='sqlite_fallback'. Both unavailable → TradingStateError. __init__.py exports state + error classes. __main__.py + render.py are Task 7. core.py target ≤300 LOC.

**Test strategy:**

test_tradingstate_integration.py: real 127.0.0.1:5434, tmp_path log. Fixture inserts shadow_trades (actual_entry_time column — NOT opened_at), recommendations, audit_reports (overall_assessment column — NOT overall_verdict), schedule_metrics. Include: 1 open live (actual_entry_time=NOW()-10m), 1 closed (excluded), 1 quarantined (excluded), 1 paper (excluded); 1 audit row 40h old with overall_assessment='STABLE' (stale=True); schedule_metrics rows: gpu_health_ollama_ok=1.0, gpu_health_training_ok=0.0. Assert: (a) state() returns dict with 1 open position (key 'entry_time' matches inserted actual_entry_time) + most_recent_audit['overall_assessment']='STABLE' + stale=True + ollama_ok=True training_ok=False + data_source='pg' + 'success' event; (b) absent gpu_health_training_ok → training_ok is None (NOT False); (c) PG down (wrong port DSN) + SQLite present → data_source='sqlite_fallback' + 'success'; (d) both PG + SQLite down → TradingStateError + 'error' event; (e) prod DSN signature → 'prod_guard_block' event AND zero duplicate 'error' events (decorator contract §4.7 — references Task 1's grep-verified _safety.py classification); (f) SNAPSHOT CONSISTENCY (DA2): fixture starts with 1 open position + 1 audit. Spawn background-thread writer that uses threading.Event to fire AFTER state() executes Query 1 but BEFORE Query 3 — writer updates shadow_trades SET status='closed' WHERE the open trade AND inserts a new audit. Synchronize via Event signaling. Call state(dsn=...) from main thread. Assert returned dict shows the position as STILL OPEN AND the audit as the OLD one (proving REPEATABLE READ pinned the snapshot at Query 1). Without REPEATABLE READ this test sees the closed position OR the new audit. Verify-by-mutation: change COALESCE(quarantined,0)=0 to =1 → quarantined-excluded assertion fails; rename SQL 'actual_entry_time AS entry_time' to 'created_at AS entry_time' → entry-time-value assertion fails (created_at differs from actual_entry_time by fixture design); remove isolation_level='REPEATABLE READ' from pg_connect → DA2 snapshot test (f) sees mutated state.

**Scope fence:** Do NOT call Alpaca API — read shadow_trades only (operator decision #2). Do NOT use opened_at (no such column). Do NOT use overall_verdict (no such column). Do NOT alias overall_assessment. Do NOT default missing schedule_metrics to False — None means 'not yet measured'. Do NOT create __main__.py or render.py — those are Task 7. Do NOT skip the REPEATABLE READ isolation level. core.py ≤300 LOC.

---

### Task 7 — Implement TradingState render + CLI (markdown rendering + __main__ + CLI test)

**Depends on:** Task 6

**Files in scope:**
- `src/tools/tradingstate/__main__.py`
- `src/tools/tradingstate/render.py`
- `tests/tools/test_tradingstate_cli.py`

**Description:**

Create src/tools/tradingstate/render.py (render_markdown(snapshot: dict) -> str with one internal function per section: _render_positions, _render_audit, _render_gpu_health) and src/tools/tradingstate/__main__.py (argparse with --dsn/--sqlite-path/--json; delegates to _cli_envelope.run_cli for error envelope per DA6; on success: if --json print json.dumps(state, default=str, indent=2), else print render_markdown(state)). Create tests/tools/test_tradingstate_cli.py — subprocess invokes python -m src.tools.tradingstate against the fixture-loaded PG (same fixture pattern as test_tradingstate_integration.py, but invoked via subprocess to exercise the full CLI path including _cli_envelope). Markdown structure: '# Trading State (as of <as_of_et>)' + '## Positions' table + '## Audit' section + '## GPU Health' section. render.py target ≤200 LOC.

**Test strategy:**

test_tradingstate_cli.py: subprocess.run(['python','-m','src.tools.tradingstate','--dsn',<test_dsn>], capture_output=True, text=True, encoding='utf-8', timeout=15). Fixture loads same rows as Task 6's integration test. Assert: (a) without --json, stdout matches markdown 3-section structure (regex: '## Positions', '## Audit', '## GPU Health' all present) + exit 0; (b) with --json, stdout parses as valid JSON with expected keys (open_positions, most_recent_audit, gpu_health, data_source, as_of_et) + exit 0; (c) ERROR ENVELOPE (DA6): forced failure via --dsn pointing to a bad port (PG down) + bad --sqlite-path (file does not exist) → with --json, stdout matches {'error': {'type': 'TradingStateError', 'message': <str>, 'tool': 'tradingstate'}} schema + exit 1; (d) without --json on the same forced failure, traceback prints to stderr + exit 1 + stdout empty. Verify-by-mutation: break render_markdown's section header → markdown test (a) fails; break the envelope schema in _cli_envelope → envelope test (c) fails.

**Scope fence:** Do NOT modify src/tools/tradingstate/core.py or queries.py — those are Task 6's contract. Do NOT add new fields to the snapshot dict — render.py is a pure formatter. Do NOT duplicate the Python-API assertions from test_tradingstate_integration.py — this test exclusively covers CLI/render. render.py ≤200 LOC.

---

### Task 8 — CHANGELOG + version bump

**Depends on:** Task 2, Task 3, Task 4, Task 5, Task 6, Task 7

**Files in scope:**
- `CHANGELOG.md`
- `src/version.py`
- `tests/test_version.py`

**Description:**

Append v0.36.62 entry to CHANGELOG.md per spec §6 (matching v0.36.57 format). Bump src/version.py VERSION constant from 'v0.36.61' to 'v0.36.62'. Update tests/test_version.py:18 _EXPECTED_VERSION literal to 'v0.36.62' ATOMICALLY in the same commit — partial updates RED tests/test_version.py. CHANGELOG calls out: five tools; per-tool subpackage layout; sub-module-when-needed pattern (TradingState core/queries/render); _cli_envelope.py for --json error envelope; data/cache/<tool-name>/ convention; CIInvestigate atomic-write + updatedAt-validated cache; TradingState REPEATABLE READ snapshot; LogTail rotation handling; DBQuery server-side streaming + jsonb warning; parity test; entry_time/actual_entry_time + overall_assessment naming notes; decorator contract (§4.7); dual-Opus QA matrix. Open() calls use encoding='utf-8'. There is NO pyproject.toml at repo root — src/version.py is the single source of truth.

**Test strategy:**

Run pytest tests/test_version.py — must PASS after both src/version.py VERSION and tests/test_version.py:18 _EXPECTED_VERSION are bumped to 'v0.36.62'. RED tests/test_version.py = non-atomic bump. Smoke check: `python -c "from src.version import VERSION; print(VERSION)"` prints v0.36.62; `python -c "from src.api.app import app"` does not raise.

**Scope fence:** Do NOT modify other CHANGELOG sections. Do NOT add Removed/Deprecated. Patch bump only (v0.36.61 → v0.36.62). Do NOT touch pyproject.toml (does not exist). Do NOT split version + test-literal across commits.

---

### Task 9 — Pre-merge integration verification

**Depends on:** Task 8

**Description:**

Run full Tier-1 test surface: pytest tests/tools/ -v. Confirm all new tests pass + keystone test_safe_op_integration.py still passes + tests/test_version.py passes. Run forbidden-imports grep across src/tools/dbquery/, logtail/, ciinvestigate/, symbolfind/, tradingstate/ + their test files for spec §2.2 patterns — any match outside docstrings/comments fails the task. Run sibling-search across src/simulation/lifecycle/prod_guard.py to confirm _PROD_SIGNATURES did not drift. Schema-drift grep: `grep -rn 'opened_at\|overall_verdict' src/tools/tradingstate/ tests/tools/test_tradingstate*.py` — both substrings MUST return zero matches (relapse to phantom columns is the canonical regression). Decorator-contract grep: `grep -rn 'prod_guard_block' tests/tools/` — must hit every tool with @prod_guard (DBQuery + TradingState). CLI smoke: `python -m src.tools.<name> --help` exits 0 for all 5 tools. CLI envelope smoke: force a failure under --json for each tool + assert stdout is valid JSON matching envelope schema.

**Test strategy:**

This task IS the test — pytest + grep + smoke. No new files. If any check fails, return to originating task (do not patch in this task — preserves task isolation for git bisect). Specifically: pytest tests/tools/ failure → originating tool task; tests/test_version.py failure → Task 8; forbidden-imports match → originating tool task; opened_at / overall_verdict grep match → Task 6 (schema relapse); missing prod_guard_block assertion → Task 2 or 6 (decorator contract gap); CLI envelope smoke failure → originating tool task.

**Scope fence:** Do NOT modify any source files. Do NOT skip failing tests. Do NOT add new tests. On failure, document originating task + stop.

---

