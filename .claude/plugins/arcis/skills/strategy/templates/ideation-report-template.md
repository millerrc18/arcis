# Ideation report — {THEME}

- **Session ID:** {SESSION_ID}
- **Captured:** {NOW_ET}
- **Theme:** {THEME}
- **Wave A agents dispatched:** {WAVE_A_LIST}
- **Wave A succeeded:** {WAVE_A_SUCCESS}
- **Wave A failed:** {WAVE_A_FAIL}
- **Wave B (cross-domain) status:** {WAVE_B_STATUS}
- **Report path:** {OUT_PATH}

> {DEGRADED_BANNER}
> (If `research-domain-lead` returned but db-investigator OR git-historian
> did not, this line is prepended verbatim: `⚠ IDEATE DEGRADED — N of M
> agents returned; synthesis is partial.` Otherwise this block is removed.)

---

## Synthesis

{SYNTHESIS}

<!--
Director composes ≤300 words from research-domain-lead's `synthesis`
field as the spine, weaves in db-investigator's `coverage_assessment`
and git-historian's commit rationale as substrate validation, and
surfaces cross-domain-analyst's tensions in a "but consider"
subsection if Wave B ran.
-->

---

## Supporting evidence ({N_SUP} findings)

<!--
Each item shape (sorted by confidence desc, then source_agent in order:
research-domain-lead → db-investigator → git-historian →
research-cross-domain-analyst):

1. [{CONFIDENCE}] {TITLE} — source: {AGENT}
   {EVIDENCE}
-->

{SUPPORTING_FINDINGS}

---

## Counter-evidence ({N_COUNTER} findings)

<!--
Same item shape as Supporting. Includes findings that argue against
the hypothesis (e.g., shelved prior strategy, alpha-decay evidence,
data-coverage gaps for the proposed substrate).
-->

{COUNTER_FINDINGS}

---

## Operational concerns ({N_OPS} findings)

<!--
Same item shape. Substrate / data / code-availability concerns
(e.g., NULL rates in the candidate signal column, missing schema
columns, plugin not yet registered).
-->

{OPERATIONAL_FINDINGS}

---

## Proposed YAML scaffold

<!--
Director emits an annotated stub from
`templates/strategy-spec-scaffold.yaml`, pre-filling:
  - strategy_id: blank for operator to fill
  - display_name: derived from theme
  - derived_from: null (MUST be present per R8 — explicit null)
  - entry.kind: best guess from theme keywords
                (event_driven if "audit" or "factor";
                 scheduled if "daily" / "weekly")
  - universe.tickers: sp100 (default)
  - position_sizing / exit blocks: TODO — fill from synthesis above
-->

```yaml
{PROPOSED_SPEC_YAML}
```

---

## Next actions

A. Open this file and refine the YAML scaffold above. Save the cleaned
   YAML under `src/platform/specs/<strategy_id>.yaml`.
B. `/arcis:strategy backtest <strategy_id> --quick` — in-sample sanity check.
C. `/arcis:strategy backtest <strategy_id>` — full walkforward (default;
   rigor-grade).

---

<sub>Generated by `/arcis:strategy ideate` ({SESSION_ID}). The
synthesis above is composed from agent reports per the merge algorithm
in `commands/strategy.md` Phase I3. Raw agent reports are not embedded
— re-run with `--keep-raw-reports` if a verbatim transcript is
required.</sub>
